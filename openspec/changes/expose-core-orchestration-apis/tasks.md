> **Path and citation convention.** Every source path is post-B2: the library lives in `subx-core/src/**` at the same relative path it had under `subx-cli/src/**` (B2 `crate-topology`, *Module Ownership Between the Two Crates*), and `cli/` plus `commands/` stay in `subx-cli/src/**` (SDR D7). Line numbers are against the tree as it stands today; A1 inserts `Reporter` fields, A2 moves `input_handler` into `core/input/` and D1 adds items to `core/sync/mod.rs`, so where a line has moved the **symbol** is the anchor, not the number.

## 1. Preconditions and Baseline

- [ ] 1.1 Confirm the sequencing window from `design.md` Decision 1: B2 landed (`subx-core/src/core/matcher/engine.rs` exists); A1 landed (`subx-core/src/core/report/mod.rs` defines `Reporter`, `NoopReporter`, `noop`, `AiUsage`, `ProgressEvent`); A2 landed (`subx-core/src/core/input/mod.rs` defines `InputPathHandler` and `CollectedFiles`); C1 landed (`subx-core/CHANGELOG.md` exists and `scripts/quality_check.sh` passes `--workspace`); D1 landed (`subx_core::core::sync::shift_subtitle_timing` exists). If any is missing, stop
- [ ] 1.2 Confirm C2a has **not** archived: `openspec/specs/core-reporting/spec.md`, `openspec/specs/input-path-handling/spec.md`, `openspec/specs/subtitle-matching/spec.md` and `openspec/specs/component-factory/spec.md` all still exist in **this** repository. If C2a has archived, stop and re-target the four core-side delta files into a change under `subx-core/openspec/changes/` per `design.md` Decision 1's fallback — the code decisions are unaffected, because Decision 7 establishes nothing here is a semver break
- [ ] 1.3 Run `scripts/quality_check.sh` once to record the pre-change verdict across both packages
- [ ] 1.4 Re-verify gap 3's evidence: `grep -rn "MatchConfig {" subx-core/src subx-cli/src` returns exactly two construction sites — `subx-core/src/core/factory.rs:70` and `subx-cli/src/commands/match_command.rs:423` — and `grep -n "relocation_mode" subx-core/src/core/factory.rs` shows the hardcoded `FileRelocationMode::None`. A third literal changes Decision 2's migration claim
- [ ] 1.5 Re-verify gap 4's evidence: `grep -rn "archive_origin" subx-cli/src/commands/` hits `convert_command.rs:362`, `match_command.rs:459`, `sync_command.rs:512`, `:585`, `translate_command.rs:330`, `:345`, `:389`. These are the five sites C2b's Decision 2 cites for *Output Directory Resolution for Archive Files*
- [ ] 1.6 Re-verify gap 6's defect: `grep -n "fn create_progress_bar" subx-cli/src/cli/ui.rs subx-cli/src/commands/match_command.rs` returns two definitions (`ui.rs:226`, `match_command.rs:807`), and `match_command.rs`'s `use` block (`:64-78`) does **not** import the `ui` helper — confirming the local definition shadows it and that `ui::progress_draw_target_for` is bypassed on the parallel path
- [ ] 1.7 Record A1's characterisation tests as the phase-7 gate: `tests/cli/match_command_json_silence.rs`, the `ai_usage_output_characterization` test, and the `translation_progress_characterization` test must all still pass unchanged at the end of this change. They are the guarantee that gap 6 adds a channel rather than moving an existing one

## 2. Gap 3 — Relocation-Mode-Aware Match Engine Construction

- [ ] 2.1 Add `pub fn match_config(&self) -> MatchConfig` to `subx-core/src/core/factory.rs`, returning the struct literal currently inline in `create_match_engine` (`factory.rs:70-79`) **verbatim**, including `confidence_threshold: 0.8` and its "Default value, can be configurable" intent
- [ ] 2.2 Add `pub fn create_match_engine_with(&self, config: MatchConfig) -> Result<MatchEngine>` to `subx-core/src/core/factory.rs`, doing what `create_match_engine` does today except that it uses the supplied `config` unmodified. It SHALL attach the factory's reporter to the produced engine on the same terms as `create_match_engine` (A1 Decision 6)
- [ ] 2.3 Rewrite `ComponentFactory::create_match_engine` (`factory.rs:69-82`) as `self.create_match_engine_with(self.match_config())`. Its signature, visibility, rustdoc contract and behaviour are unchanged; do **not** touch `ComponentFactory::new` (`factory.rs:56-59`), which is A1's locked constraint
- [ ] 2.4 Write rustdoc for both new methods with `# Arguments`, `# Returns`, `# Errors` and `# Examples` per AGENTS.md. `match_config`'s prose SHALL name the four caller-controlled fields and state that callers should prefer mutating the returned value over writing a `MatchConfig` literal; `create_match_engine_with`'s `# Examples` block SHALL compile and SHALL show the two-line relocation-mode override
- [ ] 2.5 Add a note to `MatchConfig`'s rustdoc (`subx-core/src/core/matcher/engine.rs:286`) recording that the struct is exhaustively constructible, that adding a field is therefore a major-version change, and that `ComponentFactory::match_config` is the route that makes `#[non_exhaustive]` cheap later. Do **not** add `#[non_exhaustive]` — `design.md` Decision 2 records why
- [ ] 2.6 Add unit tests in `subx-core/src/core/factory.rs`: `match_config()` field-for-field against a `TestConfigBuilder` config; `create_match_engine_with` honouring `FileRelocationMode::Copy`; `create_match_engine()` producing a `MatchConfig` identical to `match_config()`, which is the regression check that the delegation changed nothing
- [ ] 2.7 Adopt in the CLI: replace the `MatchConfig` literal at `subx-cli/src/commands/match_command.rs:423-433` with `let mut match_config = factory.match_config();` followed by the three fields the command actually chooses — `confidence_threshold`, `backup_enabled`, `relocation_mode`. `MatchEngine::new(ai_client, match_config)` at `:436` is unchanged, because the command builds its own AI client
- [ ] 2.8 Confirm `cargo check --workspace --all-targets --all-features` is clean and that no `create_match_engine` caller changed shape

## 3. Gap 4 — Archive-Aware Output Location Queries

- [ ] 3.1 Add `pub fn default_output_dir<'a>(&'a self, input: &'a Path) -> &'a Path` to `impl CollectedFiles` in `subx-core/src/core/input/mod.rs`, beside `archive_origin` (`:497`). Body: `self.archive_origin(input).and_then(Path::parent).or_else(|| input.parent()).unwrap_or(Path::new("."))` — the body of `translate_command.rs:387-391`, verbatim
- [ ] 3.2 Add `pub fn default_output_path(&self, input: &Path, extension: &str) -> PathBuf` to the same `impl`. Body: the archive arm of `convert_command.rs:362-370` verbatim (`archive.parent().unwrap_or(Path::new("."))`, stem fallback `"output"`, `join(format!("{stem}.{extension}"))`) and the `else` arm `input.with_extension(extension)` verbatim
- [ ] 3.3 Write rustdoc for both with `# Arguments`, `# Returns`, `# Examples`. **Both** entries SHALL state that `default_output_path` is deliberately not `default_output_dir(..).join(..)`, and SHALL give the concrete divergence: for the parentless input `movie.srt`, `default_output_path` yields `movie.vtt` while the join would yield `./movie.vtt`, and the rendered form reaches CLI output. Neither doc example may touch the filesystem
- [ ] 3.4 Add unit tests in `subx-core/src/core/input/mod.rs`: archive-extracted input resolving beside the archive; direct input resolving beside itself; bare relative filename yielding exactly `movie.vtt` and **not** `./movie.vtt`; an archive with no parent falling back to `.`; an input with no file stem falling back to the literal `output`; `default_output_dir` returning the archive's parent and, separately, the input's parent
- [ ] 3.5 Adopt in convert: replace the `else if let Some(archive_path) = collected.archive_origin(input_path)` and trailing `else` arms of `subx-cli/src/commands/convert_command.rs:347-371` with a single `else { collected.default_output_path(input_path, &fmt) }`. The `if let Some(ref o) = args.output` arm (`:348-361`) stays in the CLI — it is the `--output` precedence and multi-input directory-append rule the `input-path-handling` CLI half retains
- [ ] 3.6 Adopt in translate: replace the `base_dir` chain in `default_output_path` (`subx-cli/src/commands/translate_command.rs:383-392`) with `collected.default_output_dir(input_path).join(translated_file_name(input_path, target_language))`, which means the private fn takes `&CollectedFiles` instead of `Option<&Path>`. Update its one caller at `:344-348`. `explicit_output_path` (`:354-381`) and the `--replace` refusal (`:330-336`) are unchanged
- [ ] 3.7 Do **not** adopt at `subx-cli/src/commands/sync_command.rs:511-519` or `:583-591`. `design.md` Decision 3 records why: their shape is "conditionally set an `Option<PathBuf>`", and making it unconditional would change the `output: None` / `output: Some(..)` distinction that the command's overwrite handling reads. Leave a one-line comment at each site naming Decision 3, so the omission reads as a decision
- [ ] 3.8 Confirm convert's output text is byte-identical for the three cases the delta spec names: an archive-extracted input, a direct absolute input, and a bare relative input. `convert_command.rs:379` prints the resolved path, so this is the check that phase 3 changed no observable output

## 4. Gap 5 — Archive-Origin Relocation as Core Behaviour

- [ ] 4.1 Add `pub fn apply_archive_origin_relocation(operations: &mut [MatchOperation], collected: &CollectedFiles)` to `subx-core/src/core/matcher/engine.rs`, immediately beside `apply_unique_target_paths` (`:134`). Body: the loop of `subx-cli/src/commands/match_command.rs:456-466` verbatim — the `archive_origin(..).is_some() && !op.requires_relocation` guard, the `video_file.path.parent()` `if let`, and the three field writes
- [ ] 4.2 Add the `use crate::core::input::CollectedFiles;` import to `engine.rs`. This is a sideways edge inside `core/` and introduces no new dependency direction; confirm `cargo check -p subx-core` is clean
- [ ] 4.3 Write rustdoc with `# Arguments`, `# Returns` and `# Examples`, whose prose states that the function MUST be called before `apply_unique_target_paths` and why (the allocator's guarantee holds at final destinations, not at pre-rewrite candidates). Add the reciprocal cross-reference to `apply_unique_target_paths`' rustdoc. Both links are intra-crate, so they cannot trip `broken_intra_doc_links = "deny"`
- [ ] 4.4 Add unit tests in `subx-core/src/core/matcher/engine.rs`: archive-extracted subtitle forced to `Copy` into the video's parent directory; directly supplied subtitle left untouched in all three fields; an operation whose `requires_relocation` is already `true` left untouched; a video path with no parent left untouched. Reuse the fixture shape of the existing `test_unique_target_paths_archive_origin_relocation_unique` test (`:848`)
- [ ] 4.5 Adopt in the CLI: delete the loop at `subx-cli/src/commands/match_command.rs:455-466` and call `apply_archive_origin_relocation(&mut operations, &files)` in its place, keeping the `apply_unique_target_paths(&mut operations)` call at `:470` immediately after it and keeping the comment that records the ordering. Add `apply_archive_origin_relocation` to the `use crate::core::matcher::engine::{..}` list at `:70`
- [ ] 4.6 Confirm `grep -n "archive_origin" subx-cli/src/commands/match_command.rs` now returns zero hits — this is the fact that changes C2b's task 1.7 grep expectation, recorded in task 6.4

## 5. Gap 6a — Progress Vocabulary and Cancellation Query

- [ ] 5.1 Add three variants to `ProgressEvent` in `subx-core/src/core/report/mod.rs`: `Started { total: u64 }`, `Advanced { done: u64, total: u64, item: Option<&'a str> }`, `Finished { done: u64, total: u64 }`. Keep `Message(&'a str)` first and keep `#[non_exhaustive]` and the `Debug, Clone, PartialEq, Eq` derives. Every field is `u64` or `Option<&str>` deliberately — an `f32` would silently prevent the `Eq` derive from applying, which is a major-version change (`design.md` Decision 5)
- [ ] 5.2 Add `fn cancelled(&self) -> bool { false }` to `pub trait Reporter` in the same file. A **provided** method, not a required one, and **not** a supertrait — `design.md` Decision 6 records that `trait_added_supertrait` is D1's major-break hazard and that this is where it is avoided
- [ ] 5.3 Write the enum-level rustdoc stating the stream contract: one `Started`, then zero or more `Advanced`, then one `Finished`; `done` monotonic and never exceeding `total`; `done < total` at `Finished` meaning the stream stopped early; at most one open stream per reporter; a `Started` while a stream is open replaces rather than nests. Write `cancelled`'s rustdoc stating that it is polled between units of work, that returning `true` never produces an `Err`, and that mid-`.await` cancellation is achieved by dropping the future and needs no API
- [ ] 5.4 Add unit tests in `subx-core/src/core/report/mod.rs`: `NoopReporter::cancelled()` returns `false`; a `Reporter` overriding only `cancelled` still no-ops on the other four channels; two identical `Advanced` values compare equal (the `Eq` regression check); a `match` with only `Message` and `_` arms still compiles over the four-variant enum
- [ ] 5.5 Confirm `cargo check --workspace --all-targets --all-features` is clean with no source change outside `report/mod.rs` — the proof that both additions are additive

## 6. Gap 6b — Emission and Cancellation in the Execution Loops

- [ ] 6.1 In `MatchEngine::execute_operations_audit` (`subx-core/src/core/matcher/engine.rs:1743`), emit `ProgressEvent::Started { total: operations.len() as u64 }` before the loop at `:1783`, `ProgressEvent::Advanced { done, total, item: Some(&op.subtitle_file.name) }` after each operation's outcome is pushed, and `ProgressEvent::Finished { done, total }` after the loop. The dry-run early return (`:1748-1756`) SHALL emit nothing
- [ ] 6.2 In the same loop, call `self.reporter.cancelled()` before starting each operation. On `true`: stop, pad the remaining slots with `OperationOutcome { applied: false, error: None }` so `outcomes.len() == operations.len()`, emit `Finished { done, total }` with `done < total`, and return `Ok(outcomes)`. Never `Err`; never inside an operation
- [ ] 6.3 In `MatchEngine::execute_operations` (`:1557`), emit the same three events around the loop at `:1626` with `item: None`, and emit nothing on the dry-run branch (`:1562-1594`). Do **not** add a `cancelled()` check here — `design.md` Decision 6 records that `Result<()>` cannot express a short batch and that neither a signature change nor a new `SubXError` variant is permitted
- [ ] 6.4 Do **not** touch `subx-core/src/core/translation/engine.rs:395`'s `📊 Translation Progress:` emission. It stays `ProgressEvent::Message`; A1 Decision 4a froze its bytes and task 1.7's `translation_progress_characterization` test asserts them
- [ ] 6.5 Add tests in `subx-core`: the full event sequence for three operations; `item` carrying `subtitle_file.name`; an empty operations slice producing exactly `Started { total: 0 }` and `Finished { done: 0, total: 0 }`; a dry run producing no event; a reporter cancelling before item 3 of 4 leaving items 3 and 4 unattempted on disk, returning four outcomes whose last two are `{ applied: false, error: None }`, and closing with `Finished { done: 2, total: 4 }`
- [ ] 6.6 Add a test asserting that an engine with **no** reporter attached returns outcomes byte-identical to those it returned before this phase — the regression check that emission and the cancellation poll are invisible under `NoopReporter`

## 7. Gap 6c — CLI Adoption and Retiring the Duplicate Progress Bar

- [ ] 7.1 Extend `ui::create_progress_bar`'s template in `subx-cli/src/cli/ui.rs:229-235` from `… {pos}/{len} ({eta})` to `… {pos}/{len} ({eta}) {msg}`, and update the requirement's template string in its rustdoc (`:204-212`). Leave `progress_draw_target_for` (`:245`) untouched
- [ ] 7.2 Give `TerminalReporter` in `subx-cli/src/cli/reporter.rs` a `Mutex<Option<indicatif::ProgressBar>>` field and implement the three structured variants in its `progress` method: `Started` creates the bar via `ui::create_progress_bar(total)` and applies `ProgressDrawTarget::hidden()` when `general.enable_progress_bar` is false; `Advanced` sets the position and, when `item` is `Some`, the message; `Finished` finishes the bar and clears the slot. A `Started` arriving while a bar is open replaces it. `ProgressEvent::Message` keeps A1's exact behaviour
- [ ] 7.3 Give `TerminalReporter` the `enable_progress_bar` value it needs at construction time — read once from the `ConfigService` where the reporter is built, not per event. Do **not** change `TerminalReporter`'s role in A1's five command construction sites beyond this
- [ ] 7.4 Delete the private `fn create_progress_bar(total: usize)` at `subx-cli/src/commands/match_command.rs:807-813` and the `use indicatif::ProgressDrawTarget;` import at `:77`
- [ ] 7.5 Delete the bar-construction block at `subx-cli/src/commands/match_command.rs:729-737` (including its `config_service.get_config()?` read and the conditional `set_draw_target`) and rewrite `monitor_batch_execution` (`:764-805`) to take the reporter instead of `&indicatif::ProgressBar`: emit `Started { total }` before spawning, `Advanced { done: completed, total, item: None }` where it called `set_position`, and `Finished { done: completed, total }` where it called `finish_with_message`. The 500 ms ticker keeps setting the `Active: … | Queued: … | Completed: …` text, now as an `Advanced` message or a `Message` event — pick one and state it in the code comment
- [ ] 7.6 Confirm `grep -rn "ProgressBar::new" subx-cli/src/` has exactly one non-test hit, inside `ui::create_progress_bar`, and that `grep -rn "create_progress_bar\|ProgressDrawTarget" subx-cli/src/commands/` returns zero non-test hits. These are the two scenarios the `progress-reporting` delta asserts
- [ ] 7.7 Add a characterisation test asserting that `subx-cli match --output json …` over a directory with video files writes **no** progress frame to stdout or stderr with `general.enable_progress_bar = true`. This is the assertion the deleted duplicate constructor would have failed, and it is the change's `### Fixed` entry made falsifiable
- [ ] 7.8 Add a characterisation test asserting that `general.enable_progress_bar = false` in text mode produces no progress frame, and confirm every A1 characterisation test from task 1.7 still passes **unchanged**. That is the gate for this phase

## 8. Downstream Verification and the Sizing Decision Point

- [ ] 8.1 Create a scratch worktree of `../subx` outside both repositories and point its `subx-core` dependency at the local patched checkout. Nothing in this phase is committed to `../subx`; the worktree is discarded at 8.4
- [ ] 8.2 In the scratch worktree apply `design.md` Decision 10's four deletions: `commands/match.rs:259-273` (the comment and the `MatchConfig` literal) → `match_config()` plus three field writes; `commands/convert.rs:488-506` (`resolve_output_path`) → `collected.default_output_path(input, format_id)`; `commands/translate.rs:725-730` (the `base_dir` chain) → `collected.default_output_dir(input)`; `commands/match.rs:393-408` (`rewrite_archive_origins`) → `apply_archive_origin_relocation(&mut operations, &collected)`. Then `cargo check`
- [ ] 8.3 Run that repository's own tests in the worktree, in particular the concrete-path assertions at `commands/translate.rs:1240`, `:1286`, `:1308`. They are the check that `default_output_path` and `default_output_dir` reproduce the two mirrored functions bit-for-bit rather than approximately
- [ ] 8.4 Record the line count actually removed against `design.md` Decision 10's ~60, discard the worktree, and confirm `git status` in `../subx` is clean. If this phase runs long it is **cut**, not extended — it produces no committed artifact
- [ ] 8.5 **The sizing decision point** (`design.md` Decision 11). If phases 1–4 are complete and the working day is gone, stop here: lift phases 5, 6 and 7, the `specs/core-reporting/spec.md` and `specs/progress-reporting/spec.md` delta files, and their two documentation lines **verbatim** into a new change `stream-core-progress-and-cancellation`, and finish this one with three delta files. Do not compress phase 7 to fit — it is the phase with the only CLI-observable output change in the change

## 9. Specifications

- [ ] 9.1 Confirm the five delta files are the complete set and that the four core-side ones touch only requirements C2b marks **C** or that C2a migrates wholesale: `core-reporting` (wholesale core, SDR §9), `component-factory`'s *Match Engine Creation*, `subtitle-matching`'s *AI-Driven Language and Globally-Unique Target Naming*, and `input-path-handling`'s *Output Directory Resolution for Archive Files* (the one exception, reclassified — task 9.5). `progress-reporting` is CLI-only and stays in `subx-cli`
- [ ] 9.2 Verify each `## MODIFIED Requirements` restatement carries every **unchanged** scenario verbatim against `openspec/specs/{component-factory,input-path-handling,subtitle-matching,progress-reporting}/spec.md` and against A1's `core-reporting` delta. The carried-verbatim counts are: **2** for *Match Engine Creation*, **1** for *Output Directory Resolution for Archive Files* (*Explicit -o overrides archive origin*), **9** for *AI-Driven Language and Globally-Unique Target Naming* (all but the last two), and **3** for *Transport-Agnostic Reporter Seam* (*Default implementation is silent*, *Partial implementation opts into one channel*, *ProgressEvent is extensible*). A `diff` of each of those blocks against its source must be empty
- [ ] 9.3 Verify the scenarios that are deliberately **not** verbatim, and that each has a stated reason: *Transport-Agnostic Reporter Seam*'s *Reporter is usable as a trait object* changes only `crate::core::report::noop()` to `subx_core::core::report::noop()` (post-B2 path, no semantic change); *AI-Driven…*'s *Cross-video duplicates* and *Allocator runs after archive-origin forced relocation* both replace "`match_command` applies the relocation rewrite" with the named function; *Progress Bar Styling*'s single scenario changes because the template string it asserts is the thing being changed; and *Progress Bar Visibility Follows Configuration*'s two scenarios change their WHEN from "the match command constructs" to "the CLI reporter … constructs". No other scenario text differs from its source
- [ ] 9.4 Confirm the two exclusions still hold and are argued in `design.md` Decision 9: `format-conversion`'s *Input and Output Path Resolution* stays wholly true after the change (its subject is the `-i`/`--output` flags) and a delta there would state the resolution rule in a second capability; `parallel-processing`'s *Progress Reporting Opt-Out* and *Aggregated Result Reporting* carry no construction-site citation and stay true verbatim
- [ ] 9.5 Write the C2b hand-off note required by `design.md` Decision 8 into this change's completion notes: C2b's `input-path-handling` table moves from 12 C / 2 L to **13 C / 2 L (1 split)**; its Decision 3 one-per-repository split table gains a tenth row (*Output Directory Resolution for Archive Files* → core half **Archive-Aware Output Location Resolution**, CLI half keeps the title); its Decision 3 arithmetic becomes 83 removed + 8 new core-half titles = **91 core** and **59 CLI**; and its **task 1.7** grep expectation changes to "must hit `convert_command.rs`, `sync_command.rs` (twice) and `translate_command.rs`, and must **not** hit `match_command.rs`", because task 4.6 removes match's `archive_origin` call
- [ ] 9.6 Run `openspec validate expose-core-orchestration-apis --strict` and fix everything it reports

## 10. Documentation

- [ ] 10.1 Add `[Unreleased]` entries to **`subx-core/CHANGELOG.md`** (created by C1): `### Added` — `ComponentFactory::{match_config, create_match_engine_with}`; `CollectedFiles::{default_output_dir, default_output_path}`; `apply_archive_origin_relocation`; `ProgressEvent::{Started, Advanced, Finished}`; `Reporter::cancelled`. Each line SHALL note that the addition is minor-version only, and the `ProgressEvent` line SHALL name `#[non_exhaustive]` as the reason. Add a `### Changed` line recording that `MatchEngine::execute_operations_audit` now stops between operations when the attached reporter reports cancellation
- [ ] 10.2 Add `[Unreleased]` entries to **`subx-cli/CHANGELOG.md`**: `### Fixed` — progress frames were rendered in `--output json` mode on the parallel match path because `match_command.rs` defined a private progress-bar constructor that bypassed `ui::progress_draw_target_for`; `### Changed` — the shared progress-bar template gains a trailing `{msg}` segment, and the CLI reporter is now the single owner of the batch progress bar. Both belong to `subx-cli` because the defect and the visible change are both its
- [ ] 10.3 Add one sentence to `docs/configuration-guide.md`'s `general.enable_progress_bar` entry naming the CLI reporter (`subx-cli/src/cli/reporter.rs`) as the single enforcement point for the flag, replacing the per-command construction it describes today
- [ ] 10.4 Add one sentence to `docs/tech-architecture.md` recording that progress and cooperative cancellation cross the crate boundary through `subx_core::core::report::Reporter` and that no engine knows what transport renders them. Flag it for C3 in case C3 relocates it to `subx-core`'s own documentation
- [ ] 10.5 Confirm no rustdoc added in this change contains an intra-doc link crossing the crate boundary — B2's *`subx-core` Never References `subx-cli`* makes `[`subx_cli::…`]` a hard build failure under `broken_intra_doc_links = "deny"`, and phases 2–5 add rustdoc to five modules
- [ ] 10.6 Record the two `## Open Questions` findings where the next reader will meet them: the seventh duplication (`translated_file_name` and `backup_path`, mirrored at `../subx/src-tauri/src/commands/translate.rs:704-708` and `:735-742`) as a comment on `translate_command.rs`'s private helpers, and the `MatchConfig` `#[non_exhaustive]` option in the `subx-core/CHANGELOG.md` entry from task 10.1

## 11. Quality Gate

- [ ] 11.1 Run `cargo fmt` and `cargo clippy -- -D warnings` and fix all warnings
- [ ] 11.2 Run `cargo nextest run --filter-expr 'test(match_config) + test(default_output) + test(archive_origin) + test(progress) + test(cancel) + test(reporter) + test(factory)' || true` and confirm the targeted modules pass
- [ ] 11.3 Run `scripts/quality_check.sh` once at the end (main agent only — do not invoke from sub-agents) and ensure it is green
- [ ] 11.4 Run `cargo test --doc --all-features` to confirm rustdoc examples still compile
