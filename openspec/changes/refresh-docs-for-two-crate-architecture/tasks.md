## 1. Baseline and Landed-State Verification

- [ ] 1.1 Confirm C1 (`update-ci-and-release-for-two-crates`) and C2b (`split-mixed-capabilities-across-repos`) are merged, and that `openspec/split-capabilities.txt` exists with its expected twelve entries; if C2b took the seam it pre-registered (`harden-split-capability-specs`), record which capabilities are **not** yet in the file, because task 4.3's second predicate will fail on them
- [ ] 1.2 Read the landed `AGENTS.md` and `subx-core/AGENTS.md` end to end and record which of the six siblings' incremental sections actually exist and where, rather than trusting their proposals: B1's `## Repository Layout`, B2's module-guide rows and the "core may never name `subx-cli`" rule, B3's test-ownership rule and `test-support` mechanism, B4's `CARGO_BIN_EXE_subx-cli` contract and the three coverage floors replacing `:47`, C1's CI/CD and quality-script paragraphs, C2a's two-root OpenSpec paragraphs, C2b's split-capabilities paragraph
- [ ] 1.3 Confirm `CHANGELOG.md` has a `## [2.0.0]` section created by C1 and that `subx-core/CHANGELOG.md` exists with `## [1.0.0]`; record their existing sub-sections so task 7 appends `### Documentation` rather than creating a competing header
- [ ] 1.4 Run `cargo doc --workspace --all-features --no-deps --document-private-items` and then `cargo doc --workspace --all-features` once each and record the verdicts, so that any rustdoc failure attributable to C1's `--workspace` addition is separated from this change's edits before any file is touched
- [ ] 1.5 Run `scripts/quality_check.sh` once (main agent only) to record the pre-change verdict

## 2. `subx-cli/AGENTS.md`

- [ ] 2.1 Re-scope `### Layer Overview` (`:85-93`) so each of the five layers is crate-qualified: `subx-cli/src/cli/`, `subx-cli/src/commands/`, `subx-core/src/core/`, `subx-core/src/services/`, `subx-core/src/config/`, and add `subx-core/src/report/` for the `Reporter` seam A1 created
- [ ] 2.2 Re-scope `### Execution Flow` (`:76-83`) so the arrow into `core/*` and `services/*` crosses the crate boundary explicitly, and so `cli::run_with_config()` → `dispatcher` → `execute(args, &dyn ConfigService)` reads as `subx-cli` throughout
- [ ] 2.3 Add a **Crate** column to `### Module Guide` (`:112-131`) and set it per row; `src/cli/`'s `Key Types` cell (`:116`) loses `InputPathHandler`, which A2 moved to `subx_core::core::input`, and gains nothing in its place
- [ ] 2.4 Rewrite `### Common Edit Targets` (`:133-143`) with crate-qualified paths, and repair `:137`: the "Add/change CLI arguments" row drops `src/cli/validation.rs` (a 0-byte orphan A0 deleted) and re-points `src/cli/input_handler.rs` at `subx-core/src/core/input/mod.rs`; the "Add/change config keys" row (`:140`) becomes `subx-core/src/config/…`; the "Add AI provider" row (`:141`) becomes `subx-core/src/services/ai/`, `subx-core/src/core/factory.rs`, `subx-core/src/config/`; the "Add/change translation behavior" row (`:143`) splits `subx-cli/src/cli/translate_args.rs` and `subx-cli/src/commands/translate_command.rs` from `subx-core/src/core/translation/` and `subx-core/src/services/ai/`
- [ ] 2.5 Rewrite `## File Organization` (`:145-181`) as a two-repository tree: `subx-cli/` holding `.github/`, `assets/`, `docs/`, `openspec/`, `scripts/`, `src/{cli,commands,lib.rs,main.rs}`, `tests/` and the `subx-core/` submodule mount; and `subx-core/` holding its own `.config/`, `.github/`, `assets/`, `benches/`, `openspec/`, `scripts/`, `src/{core,services,config,report,error.rs,test_support,lib.rs}` and `tests/`
- [ ] 2.6 Repair the `docs/` inventory inside that tree (`:151-156`): it lists five files and omits `docs/machine-readable-output.md`, the largest document in the repository at 867 lines
- [ ] 2.7 Rewrite `### Cargo Features` (`:183-189`) to state which manifest owns each gate: `archive-rar` is `["dep:unrar"]` in `subx-core` and a pass-through `["subx-core/archive-rar"]` in `subx-cli`; `slow-tests` is the same shape; `test-support` exists only in `subx-core` and is enabled by `subx-cli`'s `[dev-dependencies]`; `default = []` in both
- [ ] 2.8 Rewrite `### Error Handling` (`:206-211`) to reflect A2: `SubXError`, its `From` impls, `category()`, `machine_code()` and `hint()` live in `subx-core/src/error.rs`; `exit_code()` and `user_friendly_message()` are methods of the `SubXErrorExt` extension trait in `subx-cli`; the exit-code range 1–6 and the "never invent ad-hoc error types" rule are unchanged
- [ ] 2.9 Rewrite the release paragraph of `## CI/CD Pipeline` (`:395-402`) to name the real **five** targets — Linux x86_64 (gnu), Linux aarch64 (gnu), Windows x86_64, macOS x86_64, macOS aarch64 — delete the two musl entries and the `SUBX_LIBC=musl` / `--musl` opt-in sentence, and state that `scripts/install.sh` rejects both musl inputs with exit code 2. This is the stale "7 targets" claim C1's task 10.3 and B1's task 8.3 both deferred here
- [ ] 2.10 Add the shared-region markers `<!-- SHARED_CONVENTIONS_START -->` and `<!-- SHARED_CONVENTIONS_END -->` around `## Coding Conventions` → `### General Rules` and `### Naming Conventions`, `### Critical Rules` of `## Testing Conventions`, `## Documentation Conventions`, `### Changelog Convention`, `### CPU-Intensive Operations — Main Agent Only`, and the core-manifest prohibitions from `design.md` Decision 2 (no `[workspace]` table, no workspace inheritance, no `[profile.*]` in `subx-core/Cargo.toml`). Nothing outside the markers is shared
- [ ] 2.11 Update `## Documentation Conventions` (`:290-298`) inside the shared region with `design.md` Decision 4's asymmetric link rule and the `--workspace` doc-build requirement
- [ ] 2.12 Verify `<!-- CODEGRAPH_START -->…<!-- CODEGRAPH_END -->` (`:412-421`) is byte-identical to its pre-change state; it is tool-managed and this change does not author it

## 3. `subx-core/AGENTS.md`

- [ ] 3.1 Read the accreted file end to end and delete every section that describes something `subx-core` does not have: any execution flow through `main.rs`, any CLI layer or command layer, any release matrix, any installer, any shell-completion material, and any reference to `scripts/install.sh` or `scripts/check_coverage.sh`
- [ ] 3.2 Write `## Project Overview` for the library: what the crate is, its repository URL `https://github.com/jim60105/subx-core`, GPL-3.0-or-later, that it has no binary target, and that its two consumers are `subx-cli` and the Tauri GUI at `jim60105/subx`
- [ ] 3.3 Write `## Build, Test, and Quality Commands` against `subx-core/scripts/quality_check.sh` — the dozen-line script C1 created, taking only an optional profile name — and state the relationship C1 fixed: the local script is a strict subset and the authoritative gate is `subx-cli/scripts/quality_check.sh` at the moment the submodule pointer is bumped
- [ ] 3.4 Write `## Architecture` and `### Module Guide` for `core/`, `services/`, `config/`, `report/`, `error.rs` and `test_support/`, with no CLI layer row
- [ ] 3.5 Write the layering rule as a prohibition: `subx-core` SHALL NOT name `subx_cli` in code, in a `use`, in a doc comment, or in an intra-doc link; and state why the last one specifically is a hard build failure in a standalone clone (`design.md` Decision 4, Rule 1)
- [ ] 3.6 Write `### Cargo Features` for core's side: `archive-rar = ["dep:unrar"]` owns the real gate; `slow-tests`; `test-support` and the optional `wiremock` / `hound` it activates; `default = []`
- [ ] 3.7 Write `## Testing Conventions` for core's side: the `test-support` module layout, the path-only self dev-dependency that lets core's own integration tests reach it, the `CARGO_MANIFEST_DIR` rule for every fixture and asset read (B3 corrected `src/services/vad/audio_loader.rs:33` and `src/core/formats/tests_support.rs:63` for exactly this), and the harness-shim requirement for nested test directories
- [ ] 3.8 Write the `openspec/` section from C2a's handoff: `subx-core/openspec/` is an independent root, the parent's tooling never descends into it, `config.yaml` is byte-identical to the parent's unmodified template, twelve capability names appear in both trees by design, and a change spanning both repositories is authored as two changes
- [ ] 3.9 Write core's CI section from C1: `subx-core/.github/workflows/build-test-audit-coverage.yml`, its three-OS `test` matrix, its own-lockfile `security` job, and its `coverage` job that measures and uploads but does not gate
- [ ] 3.10 Copy the marked shared region from `AGENTS.md` verbatim, markers included, and place it so that the surrounding document reads coherently
- [ ] 3.11 If `.codegraph/` is present in `subx-core` (B1 ships `.codegraph/.gitignore` for independent indexing), ensure the CodeGraph block is present in core's file with both markers; do not hand-author its body

## 4. The `spec-governance` Drift Check

- [ ] 4.1 Add a `check_spec_governance` step to `scripts/quality_check.sh`, placed with the other non-Cargo checks and reported through the existing `print_status` helpers so its verdict reads like every other step
- [ ] 4.2 Predicate 1 — no capability directory name appears in both `openspec/specs/` and `subx-core/openspec/specs/` unless it is present in `openspec/split-capabilities.txt`; implement the membership test with `grep -qxF` per C2b's design note, and fail naming every offending capability
- [ ] 4.3 Predicate 2 — every name in `openspec/split-capabilities.txt` is present in **both** trees; a name present in only one is a split that lost a half. Fail naming the capability and the tree it is missing from
- [ ] 4.4 Predicate 3 — the region between `<!-- SHARED_CONVENTIONS_START -->` and `<!-- SHARED_CONVENTIONS_END -->` is byte-identical in `AGENTS.md` and `subx-core/AGENTS.md`; on failure print both file paths and a `diff` of the two extracted regions. Exclude the CodeGraph block from this comparison — it is tool-managed and its two copies are written independently
- [ ] 4.5 Skip all three predicates with a warning, not a failure, when `subx-core/openspec/specs/` does not exist; a missing submodule already fails the build at manifest-parse time and a second, vaguer failure adds nothing
- [ ] 4.6 Port the same three predicates to `scripts/quality_check.ps1`, which CI runs on Windows and which `AGENTS.md:28` presents as equivalent to the Bash script
- [ ] 4.7 Prove the check red before proving it green: temporarily diverge one character inside `subx-core/AGENTS.md`'s shared region and confirm predicate 3 fails with a readable diff; temporarily create an empty `subx-core/openspec/specs/release-distribution/` and confirm predicate 1 names it; temporarily append a nonexistent name to `split-capabilities.txt` and confirm predicate 2 names it. Revert all three, then confirm green
- [ ] 4.8 Add the check's existence and its three predicates to `AGENTS.md`'s `## OpenSpec and Project Skills` section (`:379-385`), so a contributor who trips it knows what it is asserting
- [ ] 4.9 Record C2b's residual coherence gap as deliberately open, alongside the check: it catches a requirement title present in both trees but not two differently-titled requirements in one split capability's two halves that contradict each other; `design.md` Decision 7 declines C2b's proposed Purpose-cross-reference mitigation as weak

## 5. READMEs

- [ ] 5.1 `README.md:6` — add a `docs.rs/subx-core` badge beside the existing `docs.rs/subx-cli` badge, and a `crates.io/crates/subx-core` badge beside the existing `crates.io` badge; leave the two workflow badges and the codecov badge unchanged, since both workflows and the coverage report still belong to `subx-cli`
- [ ] 5.2 `README.md:137-147` `### From Source (any platform)` — change `git clone https://github.com/jim60105/subx-cli.git` to `git clone --recurse-submodules …`, and add the one-line recovery for an existing clone (`git submodule update --init --recursive`) and the `git config submodule.recurse true` note B1 identified
- [ ] 5.3 Add a short `### Using SubX as a Library` subsection after `### From Source`: `subx-core = "1"` is the library dependency, `docs.rs/subx-core` is its reference, and a library consumer should depend on `subx-core` and never on `subx-cli` — whose library surface exists only as compatibility re-exports
- [ ] 5.4 `README.md:177-188` `## Documentation` — add `subx-core`'s API reference and repository as entries, and confirm every existing `docs/…` relative link still resolves (all six remain in `subx-cli`, per `design.md` Decision 3)
- [ ] 5.5 Mirror tasks 5.1–5.4 in `README.zh-TW.md` (`:6`, `:113-123`, `:146-152`), following the file's existing parallel structure; three localisable units only
- [ ] 5.6 Verify how crates.io renders `README.md:4`'s relative `<img src="assets/logo.svg">` given that `Cargo.toml:19` lists `assets/` in `exclude` — check the live rendering of `subx-cli@1.9.1` on crates.io. If it does not resolve, replace it with an absolute raw URL in both READMEs; if it does, change nothing and record the finding
- [ ] 5.7 Author `subx-core/README.md` per `design.md` Decision 9: what the crate is, its two consumers, that a standalone clone is supported, the `archive-rar` and `test-support` features, the caret relationship to `subx-cli`'s dependency line, its own three badges (core's workflow, `crates.io/crates/subx-core`, `docs.rs/subx-core`), and GPL-3.0-or-later
- [ ] 5.8 In the same file, use **absolute** URLs for every link into the superproject (`https://github.com/jim60105/subx-cli/blob/master/docs/…`), and verify no logo, no installer instructions, no command table, no `--output json` section, no `subx-cli` workflow badge and no zh-TW counterpart file is present

## 6. Reference-Document Corrections That Contradict a Requirement

- [ ] 6.1 `docs/command-reference.md:16-24` — delete the four `-musl` rows from the release-target table, leaving the five real assets, and drop the `libc` column or reduce it to the note that gnu is implicit
- [ ] 6.2 `docs/command-reference.md:26-41` — delete the musl opt-in prose and the `SUBX_LIBC=musl bash install.sh` worked example, and replace them with the rejection contract: both `SUBX_LIBC=musl` and `--musl` exit `2` with guidance and issue no HTTP request, and musl users build from source against a locally provisioned ONNX Runtime. This is the defect `design.md` Decision 11 argues belongs in this change rather than in C4
- [ ] 6.3 `docs/machine-readable-output.md:863-865` — split the source-of-truth list: `subx-cli/src/cli/output.rs` keeps the envelope, renderer and schema-version constant; `subx-core/src/error.rs` keeps `SubXError::category`, `machine_code` and `hint`; `subx-cli/src/cli/error_ext.rs` takes `exit_code` and `user_friendly_message`, per A2
- [ ] 6.4 `docs/machine-readable-output.md:639` and `:748` — crate-qualify `src/main.rs` and `src/cli/cache_args.rs`, both of which stay in `subx-cli`
- [ ] 6.5 `docs/machine-readable-output.md:855-867` `## References` — the `openspec/changes/add-machine-readable-output/…` paths at `:857`, `:858` and the "same change directory" bullet at `:859-861` have been `openspec/changes/archive/2026-04-26-add-machine-readable-output/…` since April; repoint them, and add the note that of the capabilities named there, `error-handling`, `cache-management`, `encoding-detection`, `format-conversion`, `subtitle-matching` and `timeline-sync` are now split capabilities whose core halves live in `subx-core/openspec/specs/`, while `progress-reporting` and `machine-readable-output` stay wholly in `subx-cli`
- [ ] 6.6 `docs/configuration-guide.md:341` — crate-qualify `src/config/service.rs` as `subx-core/src/config/service.rs`
- [ ] 6.7 `docs/configuration-guide.md` — locate its statement of the adding-a-config-key checklist and crate-qualify all five destinations; cross-check against `AGENTS.md:342-351`, whose five entries must say the same thing (`subx-core/src/config/mod.rs`, `service.rs` getter **and** setter, `field_validator.rs`, `validator.rs`, and `subx-cli/docs/configuration-guide.md`)
- [ ] 6.8 `.github/skills/update-config-document/SKILL.md` — change its declared source of truth from `src/config/` to `subx-core/src/config/` (lines carrying the path in the `description` frontmatter and in the body at `:15`, `:30`, `:56`) and its primary document reference to the superproject-relative `docs/config-usage-analysis.md`. No sibling change claimed this file

## 7. Changelogs

- [ ] 7.1 Add a `### Documentation` entry to `CHANGELOG.md`'s existing `## [2.0.0]` section, summarising: both `AGENTS.md` files rewritten for the two-crate layout, the shared-conventions region and its mechanical check, the three release targets that never existed being removed from the command reference, the two-crate install and library-consumption story in both READMEs, and the corrected source paths. Do **not** touch the `## [2.0.0]` heading itself — `release.yml:29`'s `awk` extraction depends on its exact shape and C1 owns it
- [ ] 7.2 Add the corresponding `### Documentation` entry to `subx-core/CHANGELOG.md`'s `## [1.0.0]` section, scoped to core: its own `AGENTS.md` and `README.md`, and the documentation-site feature narrowing from task 8.1
- [ ] 7.3 Verify the release workflow's own extraction still produces non-empty output for `2.0.0` after the edit, by running `release.yml:29`'s `awk` expression against the edited file rather than by inspection — C1's *Release Version Bump and Changelog Header* requires exactly this check

## 8. Documentation-Site Configuration and Boundary Verification

- [ ] 8.1 Narrow `subx-core/Cargo.toml`'s `[package.metadata.docs.rs]` from `all-features = true` to an explicit `features = ["archive-rar"]`, so the published documentation site no longer activates `test-support` and no longer presents `subx_core::test_support::*` — roughly 1,930 LOC of relocated test scaffolding — as documented public API, nor pulls `wiremock` and `hound` into the documentation build. `subx-cli`'s block (`Cargo.toml:37-39`) is left unchanged; it has no test-only feature. Do not reach for `#[doc(hidden)]` — `design.md` Decision 5 rejects it, because SDR D10's items are intentionally public with out-of-crate consumers
- [ ] 8.2 Add prose to `subx-cli/src/lib.rs`'s D11 re-export block stating that `config`, `core`, `error` and `services` exist for compatibility and that `docs.rs/subx-core` is the canonical documentation. Prose only — `AGENTS.md` prohibits new `#[deprecated]`, and SDR D11 prescribes rustdoc prose as the instrument
- [ ] 8.3 Grep both crates for intra-doc links crossing the boundary in the illegal direction: `grep -rn '\[`\?subx_cli' subx-core/src/` must yield nothing in a link position; any hit becomes plain backticked prose (SDR §2.3 requires exactly two such mentions, for `hint()`'s CLI-flavoured prose and `OutputModeUnsupported`'s CLI-only construction)
- [ ] 8.4 Run `cargo doc --workspace --all-features` and then actually open `target/doc/subx_cli/index.html`, follow the `config`, `core`, `error` and `services` re-export links, and confirm each lands on a generated `subx_core` page. A green exit status is not the check — `--no-deps` satisfies the deny lint while producing dead hrefs, which is why `design.md` Decision 4 makes `--workspace` load-bearing on the doc step
- [ ] 8.5 Confirm `subx-core` builds and documents standalone: from a scratch clone of `subx-core` (not the submodule worktree), run `cargo doc --all-features --no-deps` and confirm it succeeds, which is the only place an illegal core→CLI link surfaces

## 9. Hand-Off to C4

- [ ] 9.1 Confirm this change left `docs/tech-architecture.md`, `docs/ai-provider-integration-guide.md` and `docs/config-usage-analysis.md` **untouched** — they are `refresh-reference-docs-for-two-crates`'s entire scope, and an incidental edit here would make its own baseline read (its task 1.3) unreliable
- [ ] 9.2 Confirm the three decisions C4 executes are recorded where C4 can act on them without re-deriving anything: `design.md` Decision 6 (delete the dependency block, and what replaces it), Decision 10 (demote the configuration analysis), and Decision 11's converse (`docs/command-reference.md` is repaired here and must not be re-opened there). C4's `design.md` Decision 1 carries the full inheritance table; nothing further needs recording here
- [ ] 9.3 Confirm the two cross-repository link surfaces this change created are inventoriable by C4 before it restructures anything: the absolute `blob/master/docs/…` URLs in `subx-core/README.md` (task 5.8) and the `docs/…` links in both READMEs (task 5.4). C4's task 1.2 reads them; this task only confirms they are present and enumerable

## 10. Quality Gate

- [ ] 10.1 Run `cargo fmt` and `cargo clippy -- -D warnings` and fix all warnings
- [ ] 10.2 Run `cargo nextest run --filter-expr 'test(doc) + test(version) + test(core_version)' || true` and confirm the targeted modules pass
- [ ] 10.3 Run `scripts/quality_check.sh` once at the end (main agent only — do not invoke from sub-agents) and ensure it is green
- [ ] 10.4 Run `cargo test --doc --all-features` to confirm rustdoc examples still compile

**What the gate actually verifies for a documentation change.** The four steps are the series' standard gate and are reproduced unchanged, but only two of them carry weight here and their meaning shifts:

- **10.1** is near-vacuous for Markdown edits and non-vacuous for tasks 8.1 and 8.2, which touch `subx-core/Cargo.toml` and `subx-cli/src/lib.rs`. `cargo fmt` reaches workspace members already; `clippy` reaches both crates only because C1 added `--workspace` to the script — the bare command in this step still covers the root package alone, which is why 10.3 is the authority and not this step.
- **10.2** is the weakest step of the four and is retained for form. No test in either crate asserts anything about a Markdown file, and the filter is deliberately narrow — `test(version)` and `test(core_version)` cover B1's `CORE_VERSION` major-component assertion, which is the one test that fails if task 8.1's manifest edit is malformed enough to break resolution.
- **10.3** is the real gate, and it is doing something new: after task 4, `scripts/quality_check.sh` contains the governance check, so a green run now additionally asserts that the two `openspec/` trees are disjoint outside the recorded split set, that no split lost a half, and that the two `AGENTS.md` files' shared regions are byte-identical. A red verdict from this step is now as likely to be a documentation defect as a code defect. It also runs the `--workspace` doc build (`quality_check.sh:253` after C1), which is where an illegal intra-doc link across the crate boundary becomes a failure.
- **10.4** is the step with the most residual power for a documentation change, and it is genuinely load-bearing: `broken_intra_doc_links = "deny"` means every rustdoc path edited in tasks 8.2 and 8.3 is compiled, and a doc example that names a module that moved repositories fails here rather than shipping. Note that it does **not** cover the dead-href case Decision 4 Rule 3 describes — that one is invisible to every step of this gate, which is why task 8.4 inspects the generated output by hand and task 8.5 builds core standalone.
