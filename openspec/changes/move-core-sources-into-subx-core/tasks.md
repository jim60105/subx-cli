## 1. Preconditions and Baseline

- [ ] 1.1 Confirm A0–B1 landed: `grep -rn "crate::cli" src/core src/services` returns **0** non-test hits, `src/core/input/mod.rs` and `src/cli/error_ext.rs` exist, `src/cli/input_handler.rs` and `src/cli/validation.rs` do not, `.gitmodules` names `subx-core` with `branch = main`, `git submodule status` shows no `+` prefix, and `cargo build` is green
- [ ] 1.2 Run `scripts/quality_check.sh` once as the pre-change baseline (main agent only) and record that it is green; record the workspace line-coverage percentage from the latest CI `coverage` job as the task 7.8 comparison point
- [ ] 1.3 Record the exact file inventory to be moved: `git ls-files src/core src/services src/config src/error.rs > /tmp/b2-inventory.txt`, and confirm the counts are 64 + 20 + 10 + 1 = **95** files
- [ ] 1.4 Re-verify the three greps this change's plan rests on, and stop and revise `design.md` if any disagrees: grepping the moving trees for intra-doc links of the form ``[`crate::…`]`` yields **45** links, none of which name `crate::cli` or `crate::commands`; `grep -rn 'subx_cli::' src/core src/services src/config src/error.rs` yields **98** occurrences across **30** files, none naming `subx_cli::cli`, `subx_cli::commands` or `subx_cli::App`; `grep -rcE 'crate::(core|config|error|services)' src/cli src/commands src/main.rs src/lib.rs` totals **98** lines across **17** files
- [ ] 1.5 Confirm `git filter-repo` is installed (`git filter-repo --version`); if it is not and cannot be installed, take `design.md` Decision 1's named fallback explicitly rather than silently downgrading to a plain copy
- [ ] 1.6 Check whether A2 relocated `src/error.rs`'s `#[cfg(test)] mod tests` assertions that call `exit_code()` (`:329-332`) and `user_friendly_message()` (`:338`, `:343`, and inside `test_no_api_key_leaks_in_any_variant`); if they are still in `src/error.rs`, phase 6 owns the split — this is the only body B2 edits rather than moves

## 2. Move the Sources into `subx-core`

- [ ] 2.1 Clone `subx-cli` at its current `HEAD` into a scratch directory outside the working tree and run `git filter-repo --path src/core --path src/services --path src/config --path src/error.rs`; confirm the resulting tree contains those four paths at their **unchanged** relative locations and nothing else
- [ ] 2.2 In `subx-core/`, add the filtered clone as a remote, `git fetch` it, and `git merge --allow-unrelated-histories` its `main`; the merge must be conflict-free because B1's initial commit holds only `src/lib.rs`, `Cargo.toml` and configuration, which the filtered history does not contain
- [ ] 2.3 Verify history survived: `git log --oneline -- src/core/matcher/engine.rs` inside `subx-core` returns **more than one** commit, and `git log --oneline -- src/config/service.rs` likewise
- [ ] 2.4 In `subx-cli`, `git rm -r src/core src/services src/config` and `git rm src/error.rs`; do **not** attempt `git mv` into `subx-core/` — the destination is a gitlink and cannot hold tracked files in `subx-cli`'s index
- [ ] 2.5 Confirm the moved inventory matches: `git ls-files src/core src/services src/config src/error.rs` inside `subx-core` equals `/tmp/b2-inventory.txt` from task 1.3, line for line
- [ ] 2.6 Confirm `ls src/` in `subx-cli` now shows exactly `cli/`, `commands/`, `lib.rs`, `main.rs`
- [ ] 2.7 Apply the doctest substitution to the moved files only: replace `subx_cli::` with `subx_core::` across `subx-core/src/`, then confirm `grep -rn 'subx_cli' subx-core/src/` returns **zero** hits — including in prose, since a plain-text mention of the crate name in a doctest is still a broken import
- [ ] 2.8 Confirm the substitution touched nothing in `subx-cli`: `subx-cli/src/lib.rs`'s own doctests must still say `subx_cli::`

## 3. Author `subx-core/src/lib.rs`

- [ ] 3.1 Replace B1's placeholder `subx-core/src/lib.rs` with the real crate root, keeping B1's `pub const VERSION: &str = env!("CARGO_PKG_VERSION");` verbatim (its rustdoc `# Examples` block updated from `subx_cli::VERSION` to `subx_core::VERSION`)
- [ ] 3.2 Reproduce `subx-cli/src/lib.rs:90-100`'s crate-level attribute block verbatim: `#![allow(clippy::new_without_default, clippy::manual_clamp, clippy::useless_vec, clippy::items_after_test_module, clippy::needless_borrow, clippy::uninlined_format_args, clippy::collapsible_if)]`, `#![warn(missing_docs)]`, `#![warn(rustdoc::missing_crate_level_docs)]` — `items_after_test_module` in particular, because `src/error.rs:169` opens a test module ~1,100 lines before the file ends
- [ ] 3.3 Declare `pub mod config;`, `pub mod core;`, `pub mod error;`, `pub mod services;` in that order
- [ ] 3.4 Add the root re-exports transcribed from `subx-cli/src/lib.rs:116-122`: `pub use config::Config;` and `pub use config::{ConfigService, EnvironmentProvider, ProductionConfigService, SystemEnvironmentProvider, TestConfigBuilder, TestConfigService, TestEnvironmentProvider};`, and the alias `pub type Result<T> = error::SubXResult<T>;` with its rustdoc
- [ ] 3.5 Do **not** add `pub use core::report;` or any other crate-root module alias (`design.md` Decision 2a); confirm no module is reachable by two public paths
- [ ] 3.6 Write the crate-level `//!` rustdoc: what the crate is, its GPL-3.0-or-later licence, its relationship to `subx-cli` and to the Tauri GUI, a module list covering only `config`/`core`/`error`/`services` (never `cli` or `commands`, which would be unresolvable links), the two real features `archive-rar` and `slow-tests` (dropping `subx-cli/src/lib.rs:79-84`'s stale `ai`/`audio`/`parallel` block, which names features that have never existed in the manifest), and an explicit paragraph recording Decision 2 — that `subx_core::core::…` keeps the redundant `core::` segment deliberately, to preserve the downstream consumer's paths, and that flattening it is a major version event
- [ ] 3.7 Carry across the four crate-level doctests from `subx-cli/src/lib.rs`'s header (`:28-36`, `:41-48`, `:54-63`) with their `use` lines rewritten to `subx_core::`; they are core-only in content and need no other edit
- [ ] 3.8 Move B1's `VERSION` unit test into the new crate root's `#[cfg(test)] mod tests` unchanged, so `subx-core` still has an executable test after its placeholder is replaced
- [ ] 3.9 Confirm `subx-core/src/lib.rs` contains **no** `App`, no `clap` reference, and no `cli` or `commands` module declaration

## 4. Split the Manifests

- [ ] 4.1 Add SDR §4's core set to `subx-core/Cargo.toml`'s `[dependencies]`: thiserror, anyhow, serde, serde_json, toml, config, regex, encoding_rs, url, walkdir, uuid, futures, async-trait, tokio (`rt`, `sync`, `time`, `fs`, `macros`), reqwest, symphonia (the nine explicit codecs, `default-features = false`), voice_activity_detector, rubato, audioadapter-buffers, zip, tar, flate2, sevenz-rust2, `unrar = { version = "0.5", optional = true }`, tempfile, dirs, num_cpus, log — each with the exact version requirement and feature list `subx-cli/Cargo.toml` carries today
- [ ] 4.2 Add `[dev-dependencies]` to `subx-core/Cargo.toml` containing **only** `mockall` and `wiremock` — the two crates the `#[cfg(test)]` modules inside the moved `src/**` actually use (2 and 6 sites). Do **not** add hound, rstest, test-case, pretty_assertions, tokio-test or criterion: all return zero use sites at this commit, and A0 made a zero-use-site declaration a `supply-chain-hardening` violation. B3 adds them with the tests and benches that need them (`design.md` Decision 8)
- [ ] 4.3 Set `subx-core/Cargo.toml`'s `[features]` to `default = []`, `archive-rar = ["dep:unrar"]`, `slow-tests = []`; verify the gates are real by confirming `#[cfg(feature = "archive-rar")]` appears at `subx-core/src/core/archive/rar.rs:13,113` and `mod.rs:223`, and `slow-tests` at five sites under `subx-core/src/core/formats/**`
- [ ] 4.4 Re-verify `subx-core/Cargo.toml` still has **no** `[workspace]` table, **no** `[profile.*]`, **no** `<field>.workspace = true` and **no** `[[bin]]`: `grep -nE '^\[workspace|^\[profile|\.workspace *= *true|^\[\[bin\]\]' subx-core/Cargo.toml` must return nothing. This is the manifest B1 warned would acquire inheritance during B2's authoring
- [ ] 4.5 Remove from `subx-cli/Cargo.toml`'s `[dependencies]` everything now owned by core: thiserror, regex, encoding_rs, url, walkdir, uuid, futures, async-trait, reqwest, symphonia, voice_activity_detector, rubato, audioadapter-buffers, zip, unrar, tar, flate2, sevenz-rust2, tempfile, num_cpus, config
- [ ] 4.6 Keep in `subx-cli/Cargo.toml`'s `[dependencies]`: subx-core (unchanged dual `{ path, version }` form), clap, clap_complete, colored, tabled, indicatif, env_logger, log, serde, serde_json, toml, and **`dirs`** — SDR §4 omits `dirs` from the CLI half but `src/commands/cache_command.rs:218` calls `dirs::config_dir()` in production code
- [ ] 4.7 Do **not** add `anyhow` to `subx-cli`: `grep -rnE '(^|[^a-zA-Z0-9_:])anyhow::' src/cli src/commands src/main.rs src/lib.rs` returns **0**, and the sole `anyhow` use is `SubXError::Other(#[from] anyhow::Error)` in core's `error.rs:165`
- [ ] 4.8 Change `subx-cli`'s tokio feature list to `["rt-multi-thread", "macros", "time"]` — `src/commands/match_command.rs:770` uses `tokio::time::{Duration, interval}` in production code, and `subx-cli` must not depend on core enabling `time` through feature unification
- [ ] 4.9 Set `subx-cli/Cargo.toml`'s `[features]` to `default = []`, `archive-rar = ["subx-core/archive-rar"]`, `slow-tests = ["subx-core/slow-tests"]`; confirm `slow-tests` remains a *declared* `subx-cli` feature so the four `tests/sync_*` files that write `#[cfg(feature = "slow-tests")]` still compile before B3 moves them
- [ ] 4.10 Add `tempfile` and `async-trait` to `subx-cli/Cargo.toml`'s `[dev-dependencies]` — used by `#[cfg(test)]` modules at `src/cli/sync_args.rs:474`, `src/commands/convert_command.rs:493` and `src/commands/match_command.rs:827`; leave every other existing `subx-cli` dev-dependency untouched, since B3 owns that pruning pass
- [ ] 4.11 Confirm `[profile.release]` and `[profile.dev]` are still the last two tables of `subx-cli/Cargo.toml` and exist in no other manifest, and that `[[bin]]` and both `[[bench]]` entries are unchanged
- [ ] 4.12 Run `cargo build` and let Cargo regenerate `Cargo.lock`; never hand-edit it. Confirm the total `[[package]]` count is unchanged from the task 1.2 baseline — the same registry crates are resolved, only redistributed across two manifests

## 5. Slim `subx-cli/src/lib.rs` and Repoint the CLI

- [ ] 5.1 Delete `pub mod config;`, `pub mod core;`, `pub mod error;` and `pub mod services;` from `src/lib.rs` (`:113-133`), keeping `pub mod cli;` and `pub mod commands;`
- [ ] 5.2 Add the D11 re-export block in their place: `pub use subx_core::{config, core, error, services};`, `pub use subx_core::Config;`, `pub use subx_core::{ConfigService, EnvironmentProvider, ProductionConfigService, SystemEnvironmentProvider, TestConfigBuilder, TestConfigService, TestEnvironmentProvider};`, and `pub type Result<T> = subx_core::error::SubXResult<T>;`
- [ ] 5.3 Give each re-export rustdoc marking it legacy: state that the canonical path is the `subx_core` one, that the alias exists for consumers not yet migrated, and that it will be removed once they have. Do **not** add `#[deprecated]` (AGENTS.md)
- [ ] 5.4 Add `pub use subx_core::{test_with_config, test_production_config_with_env, test_production_config_with_openai_env, create_production_config_service_with_env, create_production_config_service_with_empty_env, test_with_default_config, test_with_ai_config, test_with_ai_config_and_key, test_with_sync_config, test_with_parallel_config, create_test_config_service, create_default_test_config_service};` — all twelve `#[macro_export]` macros land at `subx_core`'s root, where `pub use subx_core::config;` cannot reach them; `tests/config_set_integration_tests.rs:4` imports one of them (`design.md` Decision 10)
- [ ] 5.5 Confirm `pub const VERSION` (`src/lib.rs:114`) is **unchanged** and is not aliased to `subx_core::VERSION`, and that B1's `pub const CORE_VERSION: &str = subx_core::VERSION;` and its `core_version_matches_declared_requirement` test are untouched — B1's `crate-topology` spec requires the two constants to stay distinct
- [ ] 5.6 Leave `App` (`:190`) and all of `new` (`:212`), `new_with_production_config` (`:236`), `run` (`:262`), `handle_command` (`:306`), `match_files` (`:340`), `convert_files` (`:382`), `sync_files` (`:440`), `sync_files_with_offset` (`:502`), `config_service` (`:527`) and `get_config` (`:539`) in `subx-cli`, unchanged — `run` calls `<cli::Cli as clap::Parser>::parse()` and four of the methods build clap `*Args` literals
- [ ] 5.7 Update `src/lib.rs`'s crate-level `//!` header: the module list (`:20-25`) now reads `cli`, `commands` plus a line stating that `config`, `core`, `error` and `services` are re-exported from the `subx_core` crate; keep B1's `subx-core` mention. Its doctests keep saying `subx_cli::` and keep compiling through the re-exports
- [ ] 5.8 Rewrite the 98 `crate::{core,config,error,services}` references across the 17 files under `src/cli/` and `src/commands/` to `subx_core::…`, including doc links: `src/cli/{mod,output,ui,convert_args,detect_encoding_args,match_args,sync_args,translate_args}.rs` and `src/commands/{cache_command,config_command,convert_command,detect_encoding_command,dispatcher,match_command,sync_command,translate_command}.rs`. They would still resolve through the re-exports; they are rewritten so deleting the re-exports later is a one-line change (`design.md` Decision 7)
- [ ] 5.9 Confirm `src/cli/sync_args.rs:27`'s `impl From<SyncMethodArg> for subx_core::core::sync::SyncMethod` compiles — it is `impl ForeignTrait<LocalType> for ForeignType`, permitted by RFC 2451, and is the only trait impl in `subx-cli` whose self type became foreign
- [ ] 5.10 Leave `src/main.rs` unchanged; confirm its three `subx_cli::` paths (`cli::output`, `cli::RunOutcome`, `config::ProductionConfigService`) still resolve, the third through the re-export, which is correct for a binary consuming its own library facade
- [ ] 5.11 Confirm `grep -rnE 'crate::(core|config|error|services)' src/` returns only `src/lib.rs`'s re-export declarations
- [ ] 5.12 Run `cargo build` at the workspace root and confirm both crates compile

## 6. Split the `error.rs` Test Module and Repoint the Boundary Guard

- [ ] 6.1 If task 1.6 found them still in place, move the `exit_code` mapping assertions (`error.rs:328-332`) and the plain `user_friendly_message` assertions (`:336-345`) into `src/cli/error_ext.rs`'s `#[cfg(test)]` module, beside A2's `Display == user_friendly_message` regression test
- [ ] 6.2 Split `test_no_api_key_leaks_in_any_variant` rather than moving it: the copy in `subx-core/src/error.rs` keeps the exhaustive variant enumeration and asserts over `Display` and `Debug` only; a mirror in `src/cli/error_ext.rs` asserts the same set over `user_friendly_message()`, importing `subx_core::services::ai::error_sanitizer::{DEFAULT_ERROR_BODY_MAX_LEN, sanitize_url_in_error, truncate_error_body}` (`services/ai/mod.rs:368` is a `pub mod`, so the path is reachable)
- [ ] 6.3 Keep both halves' "if you add a new variant, extend this list so the audit remains exhaustive" rustdoc, and add to each a pointer at the other half, so the `secrets-protection` capability's audit cannot be half-updated
- [ ] 6.4 Confirm no other `#[cfg(test)]` module in the moved trees calls `exit_code()` or `user_friendly_message()`: `grep -rn 'exit_code()\|user_friendly_message()' subx-core/src/` must return zero
- [ ] 6.5 Repoint A1's `tests/core_cli_boundary.rs` at `subx-core/src/`, resolved from `CARGO_MANIFEST_DIR` and never from the working directory, and widen its rejected-token set from `crate::cli` alone to `crate::cli`, `crate::commands` and `subx_cli` — matching anywhere in a line, comments included
- [ ] 6.6 Run the guard test and confirm it passes with zero offending files

## 7. Verify the Two-Crate Build

- [ ] 7.1 Run `cargo build -p subx-core` and `cargo build -p subx-cli`; both must succeed, and core must build without the CLI having been built first
- [ ] 7.2 Run `cargo nextest run || true` (main agent only) and confirm the test suite is green with **no test file edited** — this is the acceptance criterion for `design.md` Decision 9. Then re-verify the enumeration it rests on: every first path segment that `tests/` and `benches/` import from `subx_cli` (`config` 62, `core` 43, `cli` 42, `services` 36, `commands` 25, `Result` 7, `error` 2, `App` 2, `test_with_config` 1) resolves
- [ ] 7.3 Run `cargo bench --no-run` and confirm `benches/retry_performance.rs` (which imports `subx_cli::services::ai::retry`) and `benches/file_id_generation_bench.rs` still build — `nextest run` does not compile benches
- [ ] 7.4 Run `cargo build --features archive-rar` and confirm the pass-through reaches core's gate: the real `unrar` path at `subx-core/src/core/archive/rar.rs:21` compiles rather than the `#[cfg(not(feature = "archive-rar"))]` stub at `:113`
- [ ] 7.5 Run `cargo nextest run --features slow-tests --filter-expr 'test(mutation)' || true` and confirm the `slow-tests`-gated harnesses under `subx-core/src/core/formats/**` are compiled in
- [ ] 7.6 Run `cargo doc --no-deps --all-features` for **both** crates and confirm zero `broken_intra_doc_links` errors; then re-run `cargo doc --no-deps --all-features` from a standalone `subx-core` clone in a scratch directory **outside** the `subx-cli` tree, because that is the only configuration in which core's own `[lints.rustdoc]` block is what enforces the deny
- [ ] 7.7 In that same standalone clone, run `cargo build`, `cargo fmt -- --check`, `cargo clippy -- -D warnings` and `cargo test`; a failure here means the manifest acquired workspace inheritance while ~30 dependency lines were being added, which is invisible from inside the workspace
- [ ] 7.8 Run `cargo clippy --workspace -- -D warnings` and confirm it is clean across both crates; do **not** add `--workspace` to `scripts/quality_check.sh` — widening the scripts remains C1's, as B1 established
- [ ] 7.9 Run `cargo test --doc --all-features` at the workspace root and confirm every rewritten doctest in `subx-core` compiles and passes
- [ ] 7.10 Run `scripts/check_coverage.sh -T -p ci --lcov lcov.info` (main agent only) and confirm the combined workspace percentage matches the task 1.2 baseline within rounding — the same lines are measured under a different package name — and that the 75% floor still passes
- [ ] 7.11 Run `cargo run -- --version`, `cargo run -- --help`, and one `--output json` invocation, and confirm the output is byte-identical to before the change
- [ ] 7.12 Verify the downstream contract: for each item in SDR §8's consumed list, confirm the path resolves under `subx_core::` — spot-check `subx_core::core::matcher::{MatchEngine, MediaFile, engine::apply_unique_target_paths}`, `subx_core::core::formats::manager::FormatManager`, `subx_core::core::sync::{SyncEngine, SyncMethod, create_default_output_path}`, `subx_core::core::input::{InputPathHandler, CollectedFiles}`, `subx_core::config::{ProductionConfigService, TestConfigService, mask_sensitive_value, field_validator::normalize_ai_provider}`, `subx_core::error::SubXError::{category, hint}` and `subx_core::services::ai::AIProvider` with a scratch compile
- [ ] 7.13 Commit the `subx-core` merge, then commit the moved gitlink in `subx-cli` in the same push; confirm `git status` is clean inside `subx-core/` and `git submodule status` shows no `+` prefix, so the pointer is committed and the tree is not dirty ahead of any future publish
- [ ] 7.14 Run `cargo package --list --allow-dirty -p subx-core` and confirm the archive lists the moved sources and excludes `Cargo.lock`, `.github/` and `target/`; run the same for `subx-cli` and confirm no file under `subx-core/` appears in its archive and that the `subx-core` dependency in the generated manifest retains `version` and has lost `path`

## 8. Documentation

- [ ] 8.1 Rewrite `subx-core/README.md`: remove B1's "placeholder whose contents arrive with the source migration" note, add the real module map (`config`, `core`, `error`, `services`), state the two features, note that `git log` carries a second root from the history merge and why, and keep the relationship notes to `subx-cli` and to the Tauri GUI at `jim60105/subx`
- [ ] 8.2 Extend `subx-core/AGENTS.md` with a Module Guide covering `core/`, `services/`, `config/` and `error.rs`, and add the normative rule that no file under `src/` may name `subx_cli` or `crate::cli` in code, in a doc link, or in a doctest — with the reason (there is no dependency edge, so such a link is unfixable) and the guard test as the enforcement
- [ ] 8.3 Update `AGENTS.md` in `subx-cli`: the Module Guide and Common Edit Targets tables (`:112-144`) now place `core/`, `services/`, `config/` and `error.rs` in `subx-core`, and the row for adding a configuration key names the five files at their `subx-core/src/config/` paths (`mod.rs`, `service.rs` getter and setter, `field_validator.rs`, `validator.rs`) plus `subx-cli/docs/configuration-guide.md`
- [ ] 8.4 Update `docs/tech-architecture.md`: extend B1's two-crate topology note into the module map so each module is shown in its owning crate, and re-sync the `### Runtime Dependencies` / `### Dev Dependencies` blocks (which A0 already corrected once) against the two new manifests
- [ ] 8.5 Add a `### Changed` entry under `[Unreleased]` in `subx-cli`'s `CHANGELOG.md` recording that `subx_cli::{config, core, error, services}` are now re-exports of the `subx-core` crate, that in-crate code names `subx_core::` directly, that `archive-rar` and `slow-tests` are pass-throughs to core's gates, and that the tokio feature list gained `time`
- [ ] 8.6 Add a `### Removed` entry under `[Unreleased]` in `subx-cli`'s `CHANGELOG.md` listing the twenty-one dependencies that moved wholly to `subx-core`, and noting that `anyhow` was dropped from `subx-cli` because it has no use site there
- [ ] 8.7 Create `subx-core/CHANGELOG.md` if it does not exist and add an `[Unreleased]` → `### Added` entry recording that the crate now contains the configuration, core-engine, error and services modules migrated from `subx-cli` with their history, that its public paths are deliberately identical to the pre-split `subx_cli::` paths, and that twelve `#[macro_export]` macros are reachable at its crate root
- [ ] 8.8 Confirm no documentation file still describes `subx-cli` as a single crate owning the engines: `grep -rn 'subx_cli::core\|subx_cli::config\|subx_cli::services' docs/ AGENTS.md README.md README.zh-TW.md` returns only text that explicitly frames those paths as legacy re-exports

## 9. Quality Gate

- [ ] 9.1 Run `cargo fmt` and `cargo clippy -- -D warnings` and fix all warnings
- [ ] 9.2 Run `cargo nextest run --filter-expr 'test(config) + test(error) + test(boundary) + test(version) + test(archive)' || true` and confirm the targeted modules pass
- [ ] 9.3 Run `scripts/quality_check.sh` once at the end (main agent only — do not invoke from sub-agents) and ensure it is green
- [ ] 9.4 Run `cargo test --doc --all-features` to confirm rustdoc examples still compile
