## 1. Baseline and Classification Census

- [ ] 1.1 Confirm B2 landed: `ls subx-cli/src/` (from the repository root, `ls src/`) shows exactly `cli/`, `commands/`, `lib.rs`, `main.rs`; `subx-core/src/` holds `config/`, `core/`, `error.rs`, `services/`, `lib.rs`; `cargo nextest run --workspace` is green and `cargo bench --no-run` builds both benches
- [ ] 1.2 Re-run the ownership census and record the four counts before editing anything: for every `.rs` under `tests/` and `benches/`, classify by Decision 1's ordered rules and confirm **17** `assert_cmd` files, **8** `run_command_*` files, **39** files naming `subx_cli::cli` or `subx_cli::commands`, **42** files naming only library segments, **15** harness shims, **13** `tests/common/` modules, **2** unclassifiable (`tests/cli/input_handler_tests.rs`, `tests/config_basic_integration.rs`); if A1 has landed, expect two extra `assert_cmd` files (`tests/cli/ai_usage_output_characterization.rs`, `tests/cli/translation_progress_characterization.rs`), two extra shims, and `tests/core_cli_boundary.rs`
- [ ] 1.3 Confirm A2 moved `InputPathHandler` so that `tests/archive_input_extraction_tests.rs:16` is the only import in that file and it now names `subx_core::core::input::InputPathHandler`, making the final counts **43 core-bound / 80 CLI-bound**; if A2 left a `subx_cli::cli::InputPathHandler` legacy shim in place, rewrite the import here rather than moving the file through the shim
- [ ] 1.4 Re-derive the `tests/common/` consumer table (design.md Decision 2) with `grep -rn 'common::<module>' tests/` and confirm **zero** consumers for `validators.rs`, `parallel_helpers.rs`, `sync_helpers.rs`, `integration_test_macros.rs`, and that `tests/common/command_helpers.rs` is 3 lines of `//!` with no items
- [ ] 1.5 Re-derive the never-compiled set: list every `.rs` under `tests/cli/`, `tests/commands/`, `tests/parallel/`, `tests/sync/` and subtract the set of `#[path = "…"]` targets declared by `tests/*.rs`; confirm the **12** orphans named in design.md Decision 8
- [ ] 1.6 Record the byte sizes of the six normalisation-sensitive fixtures — `tests/fixtures/formats/{srt/basic.crlf.srt, srt/bom.srt, ass/basic.crlf.ass, ass/bom.ass, vtt/basic.crlf.vtt, vtt/bom.vtt}` — and confirm `.gitattributes` in **both** repositories carries `tests/fixtures/formats/** -text` (B1 shipped `subx-core`'s copy)
- [ ] 1.7 Record the baseline workspace coverage percentage from the last known-good CI run; do **not** run `scripts/check_coverage.sh` here (it saturates all cores and is reserved for the quality gate, and the per-crate measurement is B4's)
- [ ] 1.8 Confirm `.llvm-cov.toml` is referenced by nothing: `grep -rn 'llvm-cov.toml' scripts/ .github/ Cargo.toml subx-core/Cargo.toml` returns zero hits in both repositories

## 2. The `test-support` Mechanism and the Two Manifests

- [ ] 2.1 In `subx-core/Cargo.toml`, add `wiremock = { version = "0.6", optional = true }` and `hound = { version = "3.5", optional = true }` to `[dependencies]`, and `test-support = ["dep:wiremock", "dep:hound"]` to `[features]`; leave `default = []` and `slow-tests = []` unchanged
- [ ] 2.2 In `subx-core/Cargo.toml` `[dev-dependencies]`, add the path-only self declaration `subx-core = { path = ".", features = ["test-support"] }` — no `version` key, so `cargo package` strips it — plus `criterion = { version = "0.6.0", features = ["html_reports"] }`, `pretty_assertions = "1.4"`, `hound = "3.5"` and `wiremock = "0.6"`
- [ ] 2.3 In `subx-cli/Cargo.toml`, add `subx-core = { path = "subx-core", version = "1.0", features = ["test-support"] }` to `[dev-dependencies]` alongside the existing plain `[dependencies]` declaration; do **not** add `test-support` to `subx-cli`'s `[features]`
- [ ] 2.4 Prove the mechanism before moving anything: add a throwaway `#[cfg(feature = "test-support")] pub mod test_support { pub fn probe() -> u32 { 42 } }` to `subx-core/src/lib.rs`, a one-line test in `subx-core/tests/probe.rs` and one in `tests/probe.rs`, and confirm both pass under `cargo nextest run --workspace`
- [ ] 2.5 Prove the release-build non-leak: `cargo build --release -v 2>&1 | grep -c 'feature="test-support"'` MUST be **0**, and `cargo test --workspace --no-run -v 2>&1 | grep -c 'feature="test-support"'` MUST be non-zero; also run `cargo build --no-default-features` at the workspace root
- [ ] 2.6 Prove the packaging behaviour: `cargo package -p subx-core --allow-dirty --no-verify` then confirm the `[dev-dependencies]` table inside the produced `.crate`'s `Cargo.toml` does not name `subx-core`
- [ ] 2.7 Delete the throwaway probe module and both `probe.rs` files; if any of 2.4–2.6 failed, stop and switch to the named fallback (run core's tests with `--features test-support`, mirroring how the scripts already pass `--features slow-tests`) before proceeding
- [ ] 2.8 Delete `tokio-test`, `rstest` and `test-case` from `subx-cli`'s `[dev-dependencies]` (`Cargo.toml:153,159,160`) after re-confirming zero use sites with `grep -rlwE 'tokio_test|rstest|test_case' src tests benches subx-core/src`; do **not** add them to `subx-core`

## 3. Delete What Has No Home

- [ ] 3.1 Re-grep, then `git rm tests/common/validators.rs` (274 LOC, zero consumers; its `OutputValidator`/`ValidationResult` duplicate the copies in `cli_helpers.rs` that are actually used)
- [ ] 3.2 Re-grep, then `git rm tests/common/parallel_helpers.rs` (97 LOC) and `tests/common/sync_helpers.rs` (78 LOC)
- [ ] 3.3 Re-grep for `#[macro_use]` and for `test_with_mock_ai`/`test_with_mock_ai_error`, then `git rm tests/common/integration_test_macros.rs` (41 LOC)
- [ ] 3.4 Re-grep for `create_test_cache_files`, `create_test_config` and `create_utf8_subtitle_file`, confirm `tests/common/command_helpers.rs` defines none of them (it is three lines of `//!`), then `git rm tests/common/command_helpers.rs`; its four consumers under `tests/commands/` have never compiled and are triaged by **B4**, not here — the helper goes because `tests/common/` is this change's to reorganise, and leaving it undeclared would create exactly the orphan A0 deleted from `src/cli/validation.rs`
- [ ] 3.5 Leave `tests/commands/{cache_command_tests, config_command_tests, detect_encoding_tests, sync_command_tests}.rs` and `tests/config_basic_integration.rs` in place; B4 owns the never-compiled triage and deletes them under a single stated criterion
- [ ] 3.6 Remove the five deleted module lines (`validators`, `parallel_helpers`, `sync_helpers`, `integration_test_macros`, `command_helpers`) from `tests/common/mod.rs`, leaving `cli_helpers`, `file_managers`, `json_output`, `mock_azure_openai_helper`, `mock_generators`, `mock_openai_helper`, `test_data_generators`
- [ ] 3.7 Run `cargo nextest run --workspace` and confirm it is still green — nothing deleted in this phase had a consumer

## 4. Build `subx-core/src/test_support/`

- [ ] 4.1 Create `subx-core/src/test_support/mod.rs` declaring `pub mod workspace; pub mod file_managers; pub mod mock_openai; pub mod mock_azure_openai; pub mod mock_generators; pub mod responses;`, with a `//!` header stating that the module exists only to share fixtures across the `subx-core`/`subx-cli` repository boundary and is gated by the `test-support` feature
- [ ] 4.2 Declare it in `subx-core/src/lib.rs` as `#[cfg(feature = "test-support")] #[allow(missing_docs)] pub mod test_support;`, with rustdoc stating why the missing-documentation lint is relaxed here and that `broken_intra_doc_links = "deny"` still applies
- [ ] 4.3 Create `subx-core/src/test_support/workspace.rs` from `tests/common/cli_helpers.rs`, taking the fixture half only: `CLITestHelper` renamed to `TestWorkspace` with `new`, `with_config_service`, `with_ai_settings`, `with_sync_settings`, `temp_dir_path`, `config_service`, `create_isolated_test_workspace`, `create_media_files`, `create_subtitle_files`, `create_config_file`, `create_subtitle_file`, `create_video_file`, `test_files`, `cleanup`, `impl Default`, `impl Drop`, plus `OutputValidator` and `ValidationResult`; rewrite `subx_cli::Result` → `crate::Result` and `subx_cli::config::{ConfigService, TestConfigService}` → `crate::config::{ConfigService, TestConfigService}`
- [ ] 4.4 Do **not** carry `run_command_with_config`, `run_command_expect_success`, `assert_command_success`, `assert_command_failure` or `CommandResult` into core — `cli_helpers.rs:238` uses `env!("CARGO_MANIFEST_DIR")` with `Command::new("cargo").arg("run")`, which would expand to `subx-core` and fail
- [ ] 4.5 Create `subx-core/src/test_support/file_managers.rs` from `tests/common/file_managers.rs` (`TestFileManager`), rewriting `subx_cli::{Result, error::SubXError}` → `crate::{Result, error::SubXError}`
- [ ] 4.6 Create `subx-core/src/test_support/mock_openai.rs` from `tests/common/mock_openai_helper.rs` (`MockOpenAITestHelper`), rewriting its `crate::common::test_data_generators::MatchResponseGenerator` reference (`:56`) to `crate::test_support::responses::MatchResponseGenerator`
- [ ] 4.7 Create `subx-core/src/test_support/mock_azure_openai.rs` from `tests/common/mock_azure_openai_helper.rs` (`MockAzureOpenAITestHelper`), rewriting its `crate::common::test_data_generators` reference (`:144`) the same way
- [ ] 4.8 Create `subx-core/src/test_support/mock_generators.rs` from `tests/common/mock_generators.rs` (`DialogueSegment`, `AudioMetadata`, `AudioMockGenerator`, `SubtitleFormat`, `SubtitleEntry`, `SubtitleGenerator`), rewriting `subx_cli::{Result, error::SubXError}` → `crate::{…}`
- [ ] 4.9 Create `subx-core/src/test_support/responses.rs` from `tests/common/test_data_generators.rs` (`MatchResponseGenerator`)
- [ ] 4.10 `git rm` the seven source files under `tests/common/` that were consumed above: `cli_helpers.rs` is rewritten in phase 5, the other six (`file_managers.rs`, `mock_openai_helper.rs`, `mock_azure_openai_helper.rs`, `mock_generators.rs`, `test_data_generators.rs`) are removed outright
- [ ] 4.11 Run `cargo build -p subx-core --features test-support` and `cargo clippy -p subx-core --features test-support --all-targets -- -D warnings`; confirm `cargo build -p subx-core` (no features) still compiles and does not resolve `wiremock` or `hound`

## 5. Rewrite `subx-cli/tests/common/` and Its Consumers

- [ ] 5.1 Reduce `tests/common/mod.rs` to `pub mod cli_helpers; pub mod json_output;`
- [ ] 5.2 Rewrite `tests/common/cli_helpers.rs` to hold `CommandResult` plus `pub trait CliRun` (`run_command_with_config`, `run_command_expect_success`, `assert_command_success`, `assert_command_failure`) and `impl CliRun for subx_core::test_support::workspace::TestWorkspace`, carrying the spawning body verbatim including its `env_remove` list and `.current_dir(env!("CARGO_MANIFEST_DIR"))`
- [ ] 5.3 Leave `tests/common/json_output.rs` unchanged — `assert_json_stdout_clean` and `assert_envelope` assert the spawned binary's `--output json` stdout envelope and have no core meaning
- [ ] 5.4 Update the nine CLI-side `CLITestHelper` consumers to `use subx_core::test_support::workspace::TestWorkspace;` plus `use common::cli_helpers::CliRun;`: `tests/sync_batch_new_logic_tests.rs:3`, `tests/sync_batch_processing_integration_tests.rs:3`, `tests/sync_batch_subtitle_only_skip_tests.rs:2`, `tests/sync_comprehensive_integration_tests.rs:3`, `tests/sync_edge_cases_integration_tests.rs:3`, `tests/sync_input_path_handling_tests.rs:3`, `tests/sync_parameter_combinations_tests.rs:3`, `tests/cli/output_format_sync.rs:20`, `tests/cli/output_format_cross_command.rs:17`
- [ ] 5.5 Update the two core-side consumers to `use subx_core::test_support::workspace::{TestWorkspace, OutputValidator};` without `CliRun` — `tests/dependency_injection_integration_tests.rs:15` and `tests/vad_detector_tests.rs:151` call only fixture methods
- [ ] 5.6 Update every remaining `common::` import on the CLI side to the new paths: `common::mock_openai_helper::MockOpenAITestHelper` → `subx_core::test_support::mock_openai::MockOpenAITestHelper` (11 files) and `common::test_data_generators::MatchResponseGenerator` → `subx_core::test_support::responses::MatchResponseGenerator` (8 files); drop the now-unneeded `mod common;` from files whose only helper use was one of these
- [ ] 5.7 Update `tests/cli/output_format_sync.rs:17`'s doc link `crate::common::cli_helpers::CLITestHelper` and `tests/cli/output_format_cross_command.rs:5`'s `crate::common::json_output` reference so neither names a module that no longer exists
- [ ] 5.8 Replace the six redundant `#[path = "common/mod.rs"] mod common;` declarations with plain `mod common;` in `tests/output_format_clap_errors_tests.rs:4`, `tests/output_format_cross_command_tests.rs:4`, `tests/output_format_quiet_tests.rs:4`, `tests/output_format_sync.rs:6`, `tests/output_format_translate_success_tests.rs:4`, and any shim A1 added with the same form
- [ ] 5.9 Keep `tests/config_service_file_integration_tests.rs:3-4`'s selective inclusion, but repoint it: `TestFileManager` now comes from `subx_core::test_support::file_managers`, so replace the `#[path]` declaration with a plain `use` and delete the attribute
- [ ] 5.10 Run `cargo nextest run --workspace` and confirm green — the suite is still entirely in `subx-cli` at this point, so any failure here is a helper-split defect and not a relocation defect

## 6. Move the Core-Bound Tests, Fixtures, Benches and Assets

- [ ] 6.1 Move the **40 live** core-bound files from `tests/` to `subx-core/tests/`, preserving file names — the 43-file ownership census minus the three that have never compiled (`tests/cli/input_handler_tests.rs`, `tests/parallel/integration_tests.rs`, `tests/sync/integration_tests.rs`), which **B4** moves and revives together because flattening a file into `subx-core/tests/*.rs` is itself reviving it; the 40 include `archive_input_extraction_tests.rs` (core-bound once A2 lands `core::input`), `format_roundtrip_tests.rs`, `config_set_integration_tests.rs`, all five `hound` consumers (`vad_integration_tests.rs`, `vad_performance_tests.rs`, `vad_audio_processor_tests.rs`, `sync_engine_integration_tests.rs`, `sync_engine_performance_tests.rs`), the four `azure_openai_*` files and `wiremock_basic_integration.rs`
- [ ] 6.2 Apply `subx_cli::` → `subx_core::` across the moved files only, then confirm `grep -rn 'subx_cli' subx-core/tests/` returns zero; `tests/config_set_integration_tests.rs:4`'s `use subx_cli::test_with_config;` becomes `use subx_core::test_with_config;`, reaching the macro at `subx_core`'s crate root per B2 Decision 10
- [ ] 6.3 Rewrite the moved files' helper imports from `common::…` to `subx_core::test_support::…` and delete their `mod common;` lines; `tests/config_integration_tests.rs:6`, `tests/config_integration_tests_new.rs:6`, `tests/encoding_integration_tests.rs:12` take `file_managers::TestFileManager`, `tests/encoding_integration_tests.rs:13` takes `mock_generators::{SubtitleFormat, SubtitleGenerator}`, the four `azure_openai_*` files take `mock_azure_openai::MockAzureOpenAITestHelper` and `responses::MatchResponseGenerator`
- [ ] 6.4 Move `tests/fixtures/formats/**` (22 files) to `subx-core/tests/fixtures/formats/`; confirm `subx-core/.gitattributes` carries `tests/fixtures/formats/** -text` **before** the first `git add`, then compare the six sensitive files' byte sizes against task 1.6's record
- [ ] 6.5 Confirm `subx-core/src/core/formats/tests_support.rs:63-70`'s `CARGO_MANIFEST_DIR/tests/fixtures/formats/<rel>` now resolves, and run `cargo nextest run -p subx-core --features slow-tests --filter-expr 'test(proptest_mutated_fixtures_do_not_panic)' || true`
- [ ] 6.6 Move `benches/retry_performance.rs` and `benches/file_id_generation_bench.rs` to `subx-core/benches/`, rewrite `subx_cli::{error::SubXError, services::ai::retry::{RetryConfig, retry_with_backoff}}` → `subx_core::…` in the former, move both `[[bench]]` tables from `subx-cli/Cargo.toml:174-180` to `subx-core/Cargo.toml`, and delete `criterion`, `pretty_assertions`, `hound` and `mockall` from `subx-cli`'s `[dev-dependencies]`
- [ ] 6.7 Move `assets/SubX - The Subtitle Revolution.mp4` and `assets/SubX - The Subtitle Revolution.mp3` to `subx-core/assets/`; **copy** `assets/SubX - The Subtitle Revolution.srt` to `subx-core/assets/` and keep `subx-cli`'s copy; leave `assets/logo.svg` in `subx-cli`
- [ ] 6.8 Fix `subx-core/src/services/vad/audio_loader.rs:33` — B2 moved this `#[cfg(test)] mod tests` and its `"assets/SubX - The Subtitle Revolution.mp4"` literal now resolves against `subx-core/`; replace it with a `PathBuf::from(env!("CARGO_MANIFEST_DIR")).join("assets").join("SubX - The Subtitle Revolution.mp4")`
- [ ] 6.9 Fix the moved `subx-core/tests/sync_first_sentence_offset_integration_tests.rs:21-22` the same way for the `.mp3` and `.srt`
- [ ] 6.10 Convert the six staying `sync_*` tests' `PathBuf::from("assets/…")` literals to `CARGO_MANIFEST_DIR`-joined paths: `tests/sync_batch_new_logic_tests.rs` (6 sites), `tests/sync_batch_processing_integration_tests.rs` (6), `tests/sync_batch_subtitle_only_skip_tests.rs` (4), `tests/sync_comprehensive_integration_tests.rs` (5), `tests/sync_input_path_handling_tests.rs` (3), `tests/sync_parameter_combinations_tests.rs` (11)
- [ ] 6.11 Confirm both manifests still exclude `assets/`, `tests/` and `benches/` from the published archive, and run `cargo package --list -p subx-core --allow-dirty | grep -E '^(assets|tests|benches)/'` expecting no output
- [ ] 6.12 Add `wiremock = "0.6"` and `regex = "1.10"` to `subx-cli`'s `[dev-dependencies]` — `wiremock` is used directly by `tests/cli/{output_format_cross_command, output_format_translate_success, match_command_json_silence, output_format_match}.rs` and `regex` by `tests/cli/{match_command_json_silence, output_format_match}.rs`, and neither is a `subx-cli` normal dependency after B2

## 7. Rewrite the Staying Tests' Imports and Widen the Boundary Guard

- [ ] 7.1 Across the 80 staying files under `tests/` and every remaining file under `tests/cli/`, rewrite `subx_cli::config`, `subx_cli::core`, `subx_cli::error`, `subx_cli::services` and `subx_cli::Result` to their `subx_core::` equivalents; leave `subx_cli::cli`, `subx_cli::commands` and `subx_cli::App` alone
- [ ] 7.2 Handle the three mixed-mode files explicitly: `tests/cli/output_format_cross_command.rs:22` (`use subx_cli::core::matcher::FileDiscovery;` → `subx_core::…`), `tests/cli/output_format_config.rs:126` (a comment naming `subx_cli::config::Config`), `tests/cli/output_format_sync.rs:18` (a doc link naming `subx_cli::commands::sync_command::SyncPayload`, which stays as it is because `commands` stays)
- [ ] 7.3 Extend `tests/core_cli_boundary.rs` (added by A1, repointed by B2) with a fourth assertion: walk `subx-cli/tests/` and `subx-cli/benches/` resolved from `env!("CARGO_MANIFEST_DIR")` and assert no line contains `subx_cli::config`, `subx_cli::core`, `subx_cli::error`, `subx_cli::services`, `subx_cli::Result`, or any of the twelve `#[macro_export]` names B2 re-exported (`test_with_config`, `test_production_config_with_env`, `test_production_config_with_openai_env`, `create_production_config_service_with_env`, `create_production_config_service_with_empty_env`, `test_with_default_config`, `test_with_ai_config`, `test_with_ai_config_and_key`, `test_with_sync_config`, `test_with_parallel_config`, `create_test_config_service`, `create_default_test_config_service`) prefixed by `subx_cli::`; fail with the `file:line` list
- [ ] 7.4 Do **not** add the orphan-file check here: twelve test files are still wired to nothing when this change lands, so the assertion would ship red. **B4** adds it to the same file after triaging them
- [ ] 7.5 Confirm the D11 re-exports and the twelve macro re-exports are **not** deleted from `subx-cli/src/lib.rs`; they remain for the out-of-tree GUI, and after this phase they simply have no in-repository consumer

## 8. Verify the Split

- [ ] 8.1 `cargo build --workspace` and `cargo build --release` at the workspace root
- [ ] 8.2 `cargo nextest run --workspace` — green; then `cargo nextest run --workspace --features slow-tests` — green
- [ ] 8.3 `cargo nextest run -p subx-core --workspace=false` (or `cargo nextest run -p subx-core`) and confirm the 43 moved files run there and none of them reference `subx_cli`
- [ ] 8.4 `cargo bench --no-run --workspace` and confirm both benches build inside `subx-core`
- [ ] 8.5 `cargo doc --no-deps --all-features` in **both** crates, including from a standalone `subx-core` clone in a scratch directory **outside** the `subx-cli` tree (B1's requirement), and `cargo test --doc --all-features`
- [ ] 8.6 In that standalone clone, run `cargo test` and confirm the self dev-dependency makes `subx_core::test_support` reachable with no `--features` flag on the command line
- [ ] 8.7 `cargo clippy --workspace --all-targets --all-features -- -D warnings`
- [ ] 8.8 Re-run the release-build feature-leak count from task 2.5 and confirm it is still **0**
- [ ] 8.9 Commit the `subx-core` change and the `subx-cli` change carrying the moved gitlink together; `cargo package -p subx-core --allow-dirty=false` must not complain about a dirty submodule

## 9. Documentation

- [ ] 9.1 Add a "Which crate does my test belong to?" section to `AGENTS.md` stating Decision 1's four ordered rules, and mirror it in `subx-core/AGENTS.md`
- [ ] 9.2 Document the `test-support` mechanism in both `AGENTS.md` files: what lives in `subx_core::test_support`, that `subx-cli` enables it through `[dev-dependencies]` and never through `--features`, that `subx-core` reaches it through a path-only self dev-dependency, and that helper code with no cross-boundary consumer belongs in the owning crate's `tests/`
- [ ] 9.3 Document the harness-shim requirement in both `AGENTS.md` files — Cargo auto-discovers only `tests/*.rs`, a file under a subdirectory needs exactly one top-level `#[path]` shim, `subx-core/tests/` is flat and therefore has none — plus the `mod common;`-over-redundant-`#[path]` rule; note that twelve files are still unwired and that **B4** triages them
- [ ] 9.4 Document the `CARGO_MANIFEST_DIR` rule for every fixture and asset read in both `AGENTS.md` files, with the reason: Cargo sets a test binary's working directory to the *package* root, so a path that resolved before a module moved between packages silently stops resolving
- [ ] 9.5 Add the `--workspace` warning next to `AGENTS.md:30`'s test-command table: `default-members` is unset, so a bare `cargo nextest run` acts on the root package alone and now silently skips all 43 files under `subx-core/tests/`; leave `AGENTS.md:47`'s coverage number alone — **B4** measures the per-crate floors and replaces it
- [ ] 9.6 Update `docs/tech-architecture.md`'s testing paragraph (`:627-631`) for two crates, and its dev-dependency block for the `tokio-test`/`rstest`/`test-case` removals and the `criterion`/`pretty_assertions`/`hound` relocation
- [ ] 9.7 Add `[Unreleased]` entries to `CHANGELOG.md` in **both** repositories: `### Added` — `subx_core::test_support` behind the `test-support` feature, `subx-core`'s own `tests/`, `benches/`, `tests/fixtures/formats/` and `assets/`, and the widened boundary guard forbidding any test from resolving through the back-compatibility re-exports; `### Changed` — the 43/80 test ownership split with 40 files physically moved, `CLITestHelper` → `subx_core::test_support::workspace::TestWorkspace` with the CLI-side `CliRun` extension trait, and every fixture and asset read now resolved from `CARGO_MANIFEST_DIR`; `### Fixed` — the two working-directory-relative paths the source migration broke (`subx-core/src/services/vad/audio_loader.rs:33` and the fixture resolution behind `src/core/formats/tests_support.rs:63`); `### Removed` — five unreferenced `tests/common/` modules (493 LOC) and the `tokio-test`, `rstest` and `test-case` dev-dependencies, none of which has a use site anywhere
- [ ] 9.8 Note in both CHANGELOGs that the `subx-cli` back-compatibility re-exports survive this change and now have zero in-repository consumers, so their removal is a future major-version deletion rather than a rewrite; and that twelve never-compiled test files, the 26 runtime binary-name lookups, and the per-crate coverage floors are handed to `harden-split-test-suite` (B4)

## 10. Quality Gate

- [ ] 10.1 Run `cargo fmt` and `cargo clippy -- -D warnings` and fix all warnings
- [ ] 10.2 Run `cargo nextest run --workspace --filter-expr 'test(test_support) + test(format_roundtrip) + test(core_cli_boundary) + test(sync_batch) + test(vad) + test(azure_openai) + test(wiremock) + test(config_set) + test(dependency_injection)' || true` and confirm the targeted modules pass
- [ ] 10.3 Run `scripts/quality_check.sh` once at the end (main agent only — do not invoke from sub-agents) and ensure it is green
- [ ] 10.4 Run `cargo test --doc --all-features` to confirm rustdoc examples still compile
