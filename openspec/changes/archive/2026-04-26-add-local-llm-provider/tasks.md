## 1. Configuration Layer

- [x] 1.0 Update the rustdoc comment on the `provider` field in `src/config/mod.rs` (the `AIConfig` struct) to mention `local` alongside `openai`, `openrouter`, and `azure-openai`, and to note that `ollama` is accepted as an alias normalized to `local`. No struct/serde change is required.
- [x] 1.1 In `src/config/field_validator.rs`, add a `normalize_ai_provider(value: &str) -> String` helper that lowercases/trims input and maps `"ollama"` → `"local"`. Update the `ai.provider` allow-list to accept both `local` and `ollama` (the latter only as input; persisted value is canonical), refresh the help string from `"AI service provider (e.g., 'openai', 'azure-openai')"` to also mention `local`, and route `subx config set ai.provider <value>` through `normalize_ai_provider` so the on-disk value is always canonical.
- [x] 1.2 Add a `local` arm to `validate_ai_config` in `src/config/validator.rs` (keying off the canonical value produced by `normalize_ai_provider`): optional `api_key` (validated via `validate_api_key` only when non-empty), required non-empty `base_url` validated by `validate_url_format`, plus the standard `validate_ai_model` / `validate_temperature` / `validate_positive_number(max_tokens)` checks. Update the unsupported-provider error string to list `local`.
- [x] 1.3 In `src/config/service.rs`, add `LOCAL_LLM_BASE_URL` and `LOCAL_LLM_API_KEY` env-var handling that runs only when the canonicalized `ai.provider` is `local`, with lower precedence than `SUBX_AI_BASE_URL` / `SUBX_AI_APIKEY`.
- [x] 1.4 In `src/config/service.rs`, gate the existing hosted-provider compatibility env vars (`OPENAI_API_KEY`, `OPENAI_BASE_URL`, `OPENROUTER_API_KEY`, all `AZURE_OPENAI_*`) so they are skipped entirely when the canonicalized `ai.provider` is `local`, preserving the user's privacy choice.
- [x] 1.5 Apply `normalize_ai_provider` at every read/write site: `subx config set` (field validator, before persistence), `subx config get` (after read), `SUBX_AI_PROVIDER` env-var application in `ProductionConfigService` (before any precedence decision, including the hosted-env-var carve-out), `validate_ai_config`, and the `ComponentFactory::create_ai_provider` dispatch match. Confirm via tests that `ai.provider = "ollama"` and `SUBX_AI_PROVIDER=ollama` both reach the factory as `"local"`.
- [x] 1.6 Add unit tests in `src/config/field_validator.rs` for `normalize_ai_provider`: `"ollama"` → `"local"`, `"OLLAMA"` → `"local"`, `" ollama "` → `"local"`, canonical providers pass through, unknown values pass through unchanged so the allow-list still rejects them.
- [x] 1.7 Add unit tests in `src/config/validator.rs` for: local config without api_key validates; local config with empty base_url is rejected; `ollama` alias persisted as `local`.
- [x] 1.8 Add unit tests in `src/config/service.rs` for: `LOCAL_LLM_BASE_URL` honored when provider is local; `LOCAL_LLM_*` ignored for non-local providers; `SUBX_AI_BASE_URL` outranks `LOCAL_LLM_BASE_URL`; `OPENAI_API_KEY` / `OPENROUTER_API_KEY` / `AZURE_OPENAI_*` do not switch provider or fields when canonical provider is local; `SUBX_AI_PROVIDER=ollama` triggers the carve-out.
- [x] 1.9 In `src/config/validator.rs`, extend the hosted-provider validation arms (`openai`, `openrouter`, `azure-openai`) so that any user-set `ai.base_url` whose scheme is not `https://` is rejected with a configuration error. The error message SHALL name the field (`ai.base_url`), name the unsupported scheme, state that hosted providers require HTTPS, and append the standard local-provider hint via the helper from §1.10. Default base URLs (`https://api.openai.com/v1`, etc.) SHALL be unaffected. Add unit tests covering: HTTP rejected for openai/openrouter/azure-openai; HTTPS accepted; missing/empty base_url falls back to default; `local` provider HTTP and HTTPS both accepted.
- [x] 1.10 Add a `local_provider_hint() -> &'static str` helper (under `src/services/ai/` or `src/config/`) that returns the canonical advisory string: *"If you intended to call an OpenAI-compatible local or LAN endpoint, set `ai.provider = \"local\"` (or `ollama`) and configure `ai.base_url` to your endpoint."* Reuse this helper from §1.9 (validator) and from §3.5 (client error mapping) so the wording stays in lockstep. Add unit tests asserting the string is non-empty and contains `"local"` and `"ollama"`.

## 2. Local LLM Client

- [x] 2.1 Create `src/services/ai/local.rs` with a `LocalLLMClient` struct mirroring the structure of `src/services/ai/openrouter.rs` (fields: `client`, optional `api_key`, `model`, `temperature`, `max_tokens`, `retry_attempts`, `retry_delay_ms`, `base_url`).
- [x] 2.2 Implement `Debug` for `LocalLLMClient` redacting `api_key` as `[REDACTED]`.
- [x] 2.3 Implement `PromptBuilder`, `ResponseParser`, and `HttpRetryClient` for `LocalLLMClient`, reusing the shared trait defaults exactly as `OpenRouterClient` does.
- [x] 2.4 Implement `from_config(&AIConfig) -> Result<Self>` that: validates `base_url` is non-empty, calls `warn_on_insecure_http_str` (existing behavior already exempts loopback), and constructs the inner `reqwest::Client` with `request_timeout_seconds`.
- [x] 2.5 Implement `#[async_trait] impl AIProvider for LocalLLMClient` providing `analyze_content`, `verify_match`, and `chat_completion`. Send only the OpenAI-canonical body fields (`model`, `messages`, `temperature`, `max_tokens`). Include `Authorization: Bearer {api_key}` only when `api_key` is `Some(non_empty)`. Build the request URL by joining `base_url` with `chat/completions` such that exactly one `/` separates them — handle both trailing-slash (`http://host/v1/`) and no-trailing-slash (`http://host/v1`) forms; do not produce `//chat/completions`. Add a unit test covering both shapes.
- [x] 2.6 Map low-level errors into actionable `SubXError::AiService` messages with stable prefixes: `local LLM endpoint unreachable`, `local LLM endpoint timed out after Ns`, `local LLM endpoint returned HTTP {status}`, `local LLM model not found`, `local LLM response was not OpenAI-compatible JSON`. Sanitize bodies via `error_sanitizer` before embedding them. When embedding `base_url` in any message, route it through a dedicated `sanitize_base_url` helper that strips userinfo (`user:password@`), query strings, and fragments and returns only `scheme://host[:port]/path`; reuse the helper across all variants and unit-test it independently.
- [x] 2.7 In `src/services/ai/mod.rs`, add a `pub mod local;` declaration alongside the existing `pub mod openai; pub mod openrouter; pub mod azure_openai;` lines so the new file is compiled. Re-exporting `LocalLLMClient` at the top level is optional (the existing codebase only re-exports `OpenAIClient`); if re-exported, follow the existing `pub use openai::OpenAIClient;` pattern.

## 3. Factory Wiring

- [x] 3.1 In `ComponentFactory::create_ai_provider` (`src/core/factory.rs`), route the dispatch through `normalize_ai_provider` (so `"ollama"` reaches the dispatch as `"local"`), then add a `"local"` arm that calls `validate_ai_config` and constructs `LocalLLMClient::from_config`, returning it boxed.
- [x] 3.2 Update the unsupported-provider error string (currently `"Unsupported AI provider: {}. Supported providers: openai, openrouter, anthropic, azure-openai"`) to include `local`.
- [x] 3.3 Add a unit test `test_create_ai_provider_local_success` in `src/core/factory.rs` mirroring `test_create_ai_provider_openai_success`, asserting the returned client is the `LocalLLMClient` and that no api_key is required.
- [x] 3.4 Update `test_unsupported_ai_provider` to confirm the new error string lists `local`.
- [x] 3.5 In each hosted-provider client (`src/services/ai/openai.rs`, `openrouter.rs`, `azure_openai.rs`), wrap the existing error-emission path so that when the failure pattern matches one of the three triggers defined in the *Hosted Provider Errors Hint Toward Local Provider* requirement (HTTPS validation rejection, connection refused / DNS failure to a private host, or HTTP 200 with non-OpenAI-canonical body), the local-provider hint from §1.10 is appended to the resulting `SubXError::AiService` message via the existing `error_sanitizer` pipeline. Add a tiny `is_private_host(&Url) -> bool` helper that recognizes loopback (`127.0.0.0/8`, `::1`), RFC1918 (`10/8`, `172.16/12`, `192.168/16`), RFC4193 (`fc00::/7`), and link-local (`169.254/16`, `fe80::/10`) addresses; unit-test the helper independently.
- [x] 3.6 Add unit tests in each hosted-provider client asserting: HTTP-base-url validation error contains the hint; connection-refused against `127.0.0.1` and `192.168.0.1` (use a port with no listener) contains the hint; HTTP 200 with body `{"hello":"world"}` contains the hint; HTTP 401 from `https://api.openai.com/v1` does NOT contain the hint.

## 4. Integration Tests

> **Test placement note:** Per the repo `AGENTS.md`, nested files under `tests/cli/`, `tests/commands/`, etc. are not auto-discovered by Cargo. New integration tests below MUST land as a top-level `tests/local_llm_provider.rs` (or wire into an existing top-level harness that already includes the relevant module) so `cargo nextest` runs them. Use `MockOpenAITestHelper` (from `tests/common/mock_openai_helper.rs`) since the local endpoint speaks the OpenAI-compatible protocol; configure the mock URL via `TestConfigBuilder::with_mock_ai_server(&mock.base_url())` after setting `ai.provider = "local"`.

- [x] 4.1 Add a wiremock-backed integration test under `tests/` that stands up a local OpenAI-compatible endpoint, configures a `TestConfigService` with `ai.provider = "local"` and the mock URL, and runs the match flow end-to-end, asserting one HTTP call to `/chat/completions` and a parsed `MatchResult`.
- [x] 4.2 Add an integration test for `chat_completion` driven by the translation engine path, asserting the assistant content of the first choice round-trips correctly.
- [x] 4.3 Add an integration test for retry behavior: the wiremock returns 503 once and then 200, with `ai.retry_attempts >= 2`, asserting exactly two requests are issued.
- [x] 4.4 Add integration tests for each error-mapping branch: connection refused (no listener), HTTP 404 with "model not found" body, HTTP 200 with non-JSON body, HTTP 500 with sanitized body in the error message.
- [x] 4.5 Add an integration test asserting that when `ai.provider = "local"` and the test environment sets `OPENAI_API_KEY`, the loaded config still has `provider = "local"` and `api_key = None`. Use `TestEnvironmentProvider` to set the env var in-memory — never `std::env::set_var` (per repo testing rules).
- [x] 4.6 Add an integration test asserting that `base_url = "http://localhost:11434/v1/"` (with trailing slash) results in a request to exactly `/v1/chat/completions` (no `//chat/completions`) on the wiremock.
- [x] 4.7 Add an integration test asserting that `base_url = "http://user:secret@127.0.0.1:65500/v1?token=abc"` against an unreachable port produces a `SubXError::AiService` whose message contains `http://127.0.0.1:65500/v1` and SHALL NOT contain `secret`, `user:secret`, or `token=`.
- [x] 4.8 Add an integration test asserting that `ai.provider = "local"` with `ai.base_url = "http://192.168.50.50:11434/v1"` (a LAN address) loads successfully via `TestConfigService`, runs through `validate_ai_config` without error, and constructs a `LocalLLMClient` that targets the LAN address.
- [x] 4.9 Add an integration test asserting that `ai.provider = "openai"` with `ai.base_url = "http://localhost:11434/v1"` is rejected by `validate_ai_config` and the resulting error message contains both the substring `local` and the substring `ollama` (proving the hint is surfaced).
- [x] 4.10 Add an integration test using a wiremock-backed mock that returns HTTP 200 with body `{"hello":"world"}`. Configure `ai.provider = "openai"`, point `ai.base_url` at the mock (HTTPS via wiremock self-signed setup, or skip TLS by using a local HTTP wiremock and setting up validator to pass for the test), invoke the match flow, and assert the returned error contains the local-provider hint. Cover BOTH text-mode (`error.message` contains the hint) and JSON-mode (`--output json`: the resulting error envelope's `error.message` and/or `error.hint` contains the hint) per the *Hosted Provider Errors Hint Toward Local Provider* requirement.

## 5. Documentation

- [x] 5.1 Add a "Local / Offline LLM Provider" section to `docs/configuration-guide.md` covering `ai.provider = "local"`, optional `api_key`, required `base_url`, `LOCAL_LLM_BASE_URL` / `LOCAL_LLM_API_KEY` env vars, and example `subx config set` snippets for Ollama (`http://localhost:11434/v1`), LM Studio (`http://localhost:1234/v1`), llama.cpp `llama-server` (`http://localhost:8080/v1`), and vLLM (`http://localhost:8000/v1`). Also document that `local` is endpoint-agnostic and accepts LAN (`http://192.168.x.x:port/v1`), VPN/tailnet (`https://host.tailnet.ts.net/v1`), and remote OpenAI-compatible endpoints, with both `http://` and `https://` schemes valid. Note that hosted providers (`openai`/`openrouter`/`azure-openai`) require HTTPS and that pointing them at a local URL surfaces a hint to switch to `ai.provider = "local"`.
- [x] 5.2 Add a "Local / OpenAI-Compatible Runtimes" subsection to `docs/ai-provider-integration-guide.md` documenting the wire protocol, how `LocalLLMClient` reuses the shared traits, the privacy posture, and known compatibility limits (small models may fail strict-JSON parsing; some servers ignore `temperature` or `max_tokens`).
- [x] 5.3 Update `README.md` and `README.zh-TW.md` quick-start sections with a one-paragraph "Run with a local LLM (Ollama, LM Studio, llama.cpp)" snippet.
- [x] 5.4 Add a `### Added` entry to `CHANGELOG.md` describing the new local provider.
- [x] 5.5 Update repo `AGENTS.md` to reflect the new provider:
    - In the "Module Guide" table row for `src/services/ai/`, add `LocalLLMClient` to the listed key types.
    - In the "Supported Environment Variables → Provider-specific variables" list, add a bullet `LOCAL_LLM_API_KEY`, `LOCAL_LLM_BASE_URL` — Local LLM provider (only honored when `ai.provider = "local"`).
    - In the "AI Provider System" section, change "Three providers are supported: `openai`, `openrouter`, `azure-openai`" to "Four providers are supported: `openai`, `openrouter`, `azure-openai`, `local`" and note that `ollama` is accepted as an alias for `local`.

## 6. Quality Gate

- [x] 6.1 Run `cargo fmt` and `cargo clippy -- -D warnings`; fix every warning.
- [x] 6.2 Run `scripts/quality_check.sh` once at the end and confirm format, lint, doc-build, doc-tests, and full test suite all pass.
- [x] 6.3 Run `scripts/check_coverage.sh -T` and confirm coverage stays at or above the 75% threshold.
- [x] 6.4 Run `openspec validate add-local-llm-provider` and confirm the change validates.
