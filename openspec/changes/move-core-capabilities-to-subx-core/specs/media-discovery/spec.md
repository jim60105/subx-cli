## REMOVED Requirements

### Requirement: Recognized Video and Subtitle Extensions

**Reason**: The whole capability moves to `subx-core`. This requirement is re-added verbatim there by the `import-core-specs` change, at `openspec/specs/media-discovery/spec.md` in that repository. It leaves this repository's tree; it does not leave the project.

### Requirement: UUIDv7-Based File Identifier

**Reason**: The whole capability moves to `subx-core`. This requirement is re-added verbatim there by the `import-core-specs` change, at `openspec/specs/media-discovery/spec.md` in that repository. It leaves this repository's tree; it does not leave the project.

### Requirement: Recursion Controlled by Scan Flag

**Reason**: The whole capability moves to `subx-core`. This requirement is re-added verbatim there by the `import-core-specs` change, at `openspec/specs/media-discovery/spec.md` in that repository. It leaves this repository's tree; it does not leave the project.

**Migration**: "The recursion flag originates from the CLI `--recursive` option as consumed by the match command" is descriptive, not normative, and SHALL be qualified as `subx-cli`'s match command. The requirement itself constrains `FileDiscovery::scan_directory`, which is core.

### Requirement: MediaFile Metadata Population

**Reason**: The whole capability moves to `subx-core`. This requirement is re-added verbatim there by the `import-core-specs` change, at `openspec/specs/media-discovery/spec.md` in that repository. It leaves this repository's tree; it does not leave the project.

### Requirement: Explicit File List Scanning

**Reason**: The whole capability moves to `subx-core`. This requirement is re-added verbatim there by the `import-core-specs` change, at `openspec/specs/media-discovery/spec.md` in that repository. It leaves this repository's tree; it does not leave the project.

### Requirement: Missing Scan Root Reported as Error

**Reason**: The whole capability moves to `subx-core`. This requirement is re-added verbatim there by the `import-core-specs` change, at `openspec/specs/media-discovery/spec.md` in that repository. It leaves this repository's tree; it does not leave the project.

### Requirement: Archive Extensions Excluded from Media Classification

**Reason**: The whole capability moves to `subx-core`. This requirement is re-added verbatim there by the `import-core-specs` change, at `openspec/specs/media-discovery/spec.md` in that repository. It leaves this repository's tree; it does not leave the project.

### Requirement: Temp Directory Paths Accepted

**Reason**: The whole capability moves to `subx-core`. This requirement is re-added verbatim there by the `import-core-specs` change, at `openspec/specs/media-discovery/spec.md` in that repository. It leaves this repository's tree; it does not leave the project.

