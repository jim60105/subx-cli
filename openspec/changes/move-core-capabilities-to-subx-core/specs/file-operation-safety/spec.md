## REMOVED Requirements

### Requirement: Atomic file creation in conflict resolution

**Reason**: The whole capability moves to `subx-core`. This requirement is re-added verbatim there by the `import-core-specs` change, at `openspec/specs/file-operation-safety/spec.md` in that repository. It leaves this repository's tree; it does not leave the project.

### Requirement: Symlink detection in directory scanning

**Reason**: The whole capability moves to `subx-core`. This requirement is re-added verbatim there by the `import-core-specs` change, at `openspec/specs/file-operation-safety/spec.md` in that repository. It leaves this repository's tree; it does not leave the project.

### Requirement: Symlink protection on write targets with parent-chain validation

**Reason**: The whole capability moves to `subx-core`. This requirement is re-added verbatim there by the `import-core-specs` change, at `openspec/specs/file-operation-safety/spec.md` in that repository. It leaves this repository's tree; it does not leave the project.

### Requirement: Safe rename operations

**Reason**: The whole capability moves to `subx-core`. This requirement is re-added verbatim there by the `import-core-specs` change, at `openspec/specs/file-operation-safety/spec.md` in that repository. It leaves this repository's tree; it does not leave the project.

