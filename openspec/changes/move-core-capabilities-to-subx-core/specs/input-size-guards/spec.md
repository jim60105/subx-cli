## REMOVED Requirements

### Requirement: Subtitle file size limit

**Reason**: The whole capability moves to `subx-core`. This requirement is re-added verbatim there by the `import-core-specs` change, at `openspec/specs/input-size-guards/spec.md` in that repository. It leaves this repository's tree; it does not leave the project.

### Requirement: Audio file size limit

**Reason**: The whole capability moves to `subx-core`. This requirement is re-added verbatim there by the `import-core-specs` change, at `openspec/specs/input-size-guards/spec.md` in that repository. It leaves this repository's tree; it does not leave the project.

### Requirement: AI response body size limit

**Reason**: The whole capability moves to `subx-core`. This requirement is re-added verbatim there by the `import-core-specs` change, at `openspec/specs/input-size-guards/spec.md` in that repository. It leaves this repository's tree; it does not leave the project.

